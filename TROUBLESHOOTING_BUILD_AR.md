# حل مشاكل البناء - Desktop Management System v1.0.0

## المشكلة الحالية ⚠️

```
⨯ remove app.asar: The process cannot access the file because it is being used by another process.
```

**السبب:** التطبيق أو Electron يعمل في الخلفية ويقفل ملفات البناء.

## الحل السريع 🚀

### الطريقة 1: استخدام Force Build Script (موصى به) ⭐

أنشأت لك script جديد يحل المشكلة تلقائياً:

1. **أغلق جميع النوافذ المفتوحة:**

   - أغلق File Explorer (مستكشف الملفات)
   - أغلق VS Code إذا كان مفتوحاً
   - أغلق أي نافذة أخرى تعرض مجلد المشروع

2. **شغل الـ script الجديد:**
   - ابحث عن ملف: `force-build.bat`
   - **انقر بزر الماوس الأيمن**
   - اختر **"Run as administrator"**
   - انتظر حتى ينتهي (5-10 دقائق)

### الطريقة 2: الحل اليدوي

إذا لم يعمل الـ script، اتبع هذه الخطوات:

#### الخطوة 1: إيقاف جميع العمليات

افتح **Task Manager** (مدير المهام):

- اضغط `Ctrl + Shift + Esc`
- ابحث عن:
  - `electron.exe` - أنهي العملية
  - `node.exe` - أنهي العملية
  - `نظام إدارة شركة المقاولات` - أنهي العملية

#### الخطوة 2: حذف مجلد البناء

افتح **Command Prompt as Administrator**:

1. اضغط `Windows + X`
2. اختر **"Command Prompt (Admin)"** أو **"PowerShell (Admin)"**
3. نفذ:

   ```cmd
   cd "C:\Users\ammn\Desktop\MBM_app\Final_5Sep\Desktop Management System (Community) (Copy) (Copy)"

   rmdir /s /q "build\electron"

   timeout /t 3
   ```

#### الخطوة 3: إعادة البناء

```cmd
npm run build:electron
```

انتظر 5-10 دقائق حتى ينتهي البناء.

## المشاكل الشائعة وحلولها 🔧

### 1. "Cannot create symbolic link"

**السبب:** تحتاج صلاحيات Administrator

**الحل:** تأكد من تشغيل الـ script بصلاحيات Administrator (زر الماوس الأيمن → Run as administrator)

### 2. "File is being used by another process"

**السبب:** البرنامج يعمل في الخلفية

**الحل:**

1. أغلق جميع نوافذ التطبيق
2. افتح Task Manager وأنهي جميع عمليات `electron.exe` و `node.exe`
3. أغلق File Explorer
4. شغل `force-build.bat` مرة أخرى

### 3. "ENOENT: no such file or directory, open 'C:\Windows\System32\package.json'"

**السبب:** الـ script يعمل من مجلد خاطئ

**الحل:** تم إصلاحه في `force-build.bat` - يتحقق من المجلد تلقائياً

### 4. Build يتوقف عند "packaging"

**السبب:** قد يكون الـ antivirus يحجب electron-builder

**الحل:**

1. أضف استثناء (exclusion) في Windows Defender أو الـ antivirus:
   - مجلد المشروع: `C:\Users\ammn\Desktop\MBM_app\Final_5Sep\`
   - مجلد cache: `C:\Users\ammn\AppData\Local\electron-builder\`
2. أعد المحاولة

### 5. البناء يفشل بخطأ "out of memory"

**السبب:** الذاكرة غير كافية

**الحل:**

1. أغلق جميع البرامج غير الضرورية
2. أضف متغير بيئة:
   ```cmd
   set NODE_OPTIONS=--max-old-space-size=8192
   npm run build:electron
   ```

## التحقق من نجاح البناء ✅

بعد نجاح البناء، تحقق من:

1. **وجود الملفات:**

   ```cmd
   dir "build\electron\*.exe"
   dir "build\electron\latest.yml"
   ```

2. **حجم الملف المناسب:**

   - `DesktopManagementSystem-1.0.0-win-x64.exe` يجب أن يكون بين 150-250 MB
   - إذا كان أقل من 100 MB، قد يكون هناك مشكلة

3. **تجربة الـ installer:**
   - شغل الـ installer في مجلد اختبار
   - تأكد من أن التطبيق يعمل بشكل صحيح

## الملفات المساعدة 📁

- `force-build.bat` - Script بناء قوي مع معالجة الأخطاء
- `build-as-admin.bat` - Script البناء الأساسي
- `BUILD_INSTRUCTIONS_AR.md` - تعليمات النشر الكاملة

## الدعم 💬

إذا استمرت المشكلة:

1. تحقق من ملفات الأخطاء:

   - `C:\Users\ammn\AppData\Local\npm-cache\_logs\`
   - ابحث عن أحدث ملف log

2. تحقق من أن لديك:

   - Node.js 18+ مثبت
   - npm 9+ مثبت
   - مساحة كافية على القرص (5+ GB)
   - ذاكرة كافية (8+ GB RAM)

3. حاول تشغيل:
   ```cmd
   npm cache clean --force
   npm install
   npm run build:electron
   ```

---

**آخر تحديث:** 29 أكتوبر 2025
**تم الإعداد بواسطة:** Claude Code 🤖
