# سجل التغييرات

يتتبع هذا المستند أبرز التغييرات القابلة للملاحظة في منصة إدارة المكتب. يتم سرد الإصدارات بترتيب تنازلي، مع الإشارة إلى الترحيلات أو الاختبارات المرتبطة لضمان سهولة التتبع.

## 10 أكتوبر 2025 - توسعة اختبارات المنصة وتحديث التكوينات

### 🧪 تغطية الاختبارات

- إضافة حزمة اختبارات شاملة تغطي المستودعات المحلية (`Local*Repository`)، الخدمات المركزية (`pricingDataSyncService`, `tenderSubmissionService`, `developmentStatsService`، إلخ)، وواجهات المستخدم الحرجة (`SimplifiedProjectCostView`, `ProjectCostView`, `FinancialSummaryCard`).
- إدخال اختبارات سياسات وضوابط أمنية (`ipcGuard`, `desktopSecurity`, `storageLifecycle`, `backupManager`, `secureStore`) لضمان سلامة الجسور مع Electron في وضع سطح المكتب وبديل المتصفح.
- تعزيز اختبارات النطاق المالي والتحليلي عبر وحدات `analytics`, `domain`, و`hooks`, مع مقارنة نتائج المعالجات القديمة والجديدة لضمان التطابق الحسابي.
- تشغيل كامل للحزمة الجديدة باستخدام `npm run -s test` مع **186/186** اختبارًا ناجحًا (مدة 55.09 ثانية).

### 🛠️ تحديثات التكوين والبنية

- إعادة ضبط `tsconfig.json` و`vitest.config.ts` لتفعيل الوضع الصارم، تعريف المسارات (`@/*`)، وإدماج ملحق React SWC لتحسين سرعة التحويل أثناء الاختبارات.
- مواءمة `vite.config.ts` مع بيئة سطح المكتب عبر ضبط المنافذ، رأس `Content-Security-Policy` للتطوير، وتقسيم الحزم (`splitVendorChunkPlugin`, `rollup manualChunks`) لتسريع البناء والتحزيم.
- توسيع إعداد `tsconfig.node.json` لدعم build scripts مع `allowSyntheticDefaultImports` وضبط `strict` لتوحيد التحقق عبر المشاريع.

---

## 10 أكتوبر 2025 - موجة التوافق 4: نموذج المنافسة

### ✨ تحسينات رئيسية (نموذج المنافسة)

- إعادة بناء `NewTenderForm` لاستخدام الألوان الدلالية الموحدة في التحليلات، بطاقات المرفقات، وقائمة المراجعة، مع استبدال حالات `bg-orange-*`, `text-gray-*`, و`bg-red-100` بـ Tokens (`bg-warning/10`, `text-success`, `bg-destructive/10`) المتوافقة مع السمات الثلاث.
- تحديث منطقة رفع الملفات وجداول الكميات على حدود `border-border` وخلفيات `bg-muted` بحيث تتكيف مع الوضع الداكن دون الحاجة إلى شروط صريحة.
- ضبط شارات المؤشرات (`Badge`) لتستهلك حالات `info`, `success`, `warning`, `destructive` الموحدة، وربط تحليل المواعيد والقيمة بمؤشرات لونية متسقة مع باقي النماذج.

### 📚 التوثيق

- توثيق حالة `NewTenderForm` في `docs/design-system/COMPONENT_INVENTORY.md` باعتباره المرجع الجديد لبقية نماذج المنافسات ضمن موجة التوافق الحالية.

### 🧪 الاختبارات (نموذج المنافسة)

- **186/186** اختبار Vitest ناجح بعد التعديلات (`npm run -s test`، مدة 46.17 ثانية).

---

## 10 أكتوبر 2025 - موجة التوافق 4: نموذج المشروع

### ✨ تحسينات رئيسية (نموذج المشروع)

- إعادة بناء `NewProjectForm` لاستخدام الألوان الدلالية الكاملة: تحويل بطاقات التقديرات المالية، شارات مستوى التعقيد، وقوائم التلميحات إلى `bg-info/10`, `text-success`, `border-warning/20` وغيرها من Tokens الموحدة مع إزالة الاعتماد على درجات Tailwind الخام.
- توحيد قائمة المراجعة وخطوات ما بعد الإدخال على متغيرات التصميم (`bg-success`, `bg-warning`, `bg-accent`) مع ضبط حالات النصوص على `text-muted-foreground` لضمان قابلية القراءة في جميع السمات.
- تكييف حوارات التأكيد (`AlertDialog`) مع متغير `destructive` القياسي وإعادة استخدام عناصر `select` بعناصر حدود `border-input` لمواءمة الجهاز البصري للنماذج.

### 📚 التوثيق

- تحديث `docs/design-system/COMPONENT_INVENTORY.md` لتوثيق توافق `NewProjectForm` واعتماد خطة الاستفادة منه كنموذج للموجات القادمة من النماذج.

### 🧪 الاختبارات (نموذج المشروع)

- **186/186** اختبار Vitest ناجح بعد التعديلات (`npm run -s test`، مدة 46.34 ثانية).

---

## 9 أكتوبر 2025 - موجة التوافق 4: صفحة المشاريع

### ✨ تحسينات رئيسية

- توحيد صفحة `Projects` بالكامل على نظام الألوان الدلالية: استبدال `text-green-600`, `text-red-600`, `bg-amber-*` بـ `text-success`, `text-destructive`, `bg-warning/10` وغيرها من Tokens الموحدة.
- تحديث بطاقات التحليل والإحصائيات السريعة لتستخدم `DetailCard` و`Badge` مع الظلال والحدود الدلالية، وضبط تبويبات التصنيف لتدعم حالات فعالة/غير فعالة متناسقة مع التصميم الجديد.
- تحسين تنبيه إدخال التكلفة الفعلية للمشاريع المكتملة ليستخدم حدود وخلفية `warning` الموحدة، مع الحفاظ على تجربة RTL وتوافق القراءة.
- تهيئة حوارات الحذف (`AlertDialog`) لتستفيد من ألوان `destructive` الموحدة بدلاً من الدرجات الحمراء الصريحة.
- إعادة تصميم صفحة `Tenders` على tokens الهوية: تحديث `TenderCard`, التبويبات، البطاقة التحليلية، والتنبيهات لتستخدم `success/warning/destructive/info` الموحدة مع إزالة درجات Tailwind الخام (`bg-green-50`, `text-orange-500`, `bg-blue-50`).

### 🧪 الاختبارات

- **186/186** اختبار Vitest ناجح بعد التعديلات (`npm run -s test`).

---

## 7 أكتوبر 2025 - إكمال المرحلة 3: التنقل ومسارات العمل

### ✨ الميزات الجديدة

#### W3.1 & W3.2: مخطط التنقل الموحد

- مخطط رسمي `NavigationSchema` في `src/application/navigation/navigationSchema.ts` يحدد جميع الصفحات، الأذونات، والإجراءات السريعة
- ربط `NavigationProvider` بالمخطط لتوليد القوائم الديناميكية حسب صلاحيات المستخدم
- تحديث `Sidebar` لاستهلاك المخطط بشكل ديناميكي
- تفعيل Breadcrumb في `Header` حسب تعريفات المخطط
- تحميل الواجهات عبر lazy loading لتحسين الأداء

#### W3.3: معالج تسعير المنافسات متعدد المراحل

- معالج `TenderPricingWizard` شامل في `src/features/tenders/pricing/` مع 5 مراحل:
  1. التسجيل (Registration)
  2. الملفات الفنية (Technical Files)
  3. الملفات المالية (Financial Files)
  4. المراجعة (Review)
  5. الإرسال (Submit)
- حفظ تلقائي ذكي في `localStorage` باستخدام `STORAGE_KEYS.TENDER_PRICING_WIZARDS`
- إمكانية استئناف المسودات من لوحة المنافسات
- تسجيل كامل لجميع الأحداث في `auditLog` مع وسوم `tender-pricing-wizard`
- سيناريو Playwright `tests/e2e/desktop/tender-pricing-wizard.spec.ts` يغطي المسار الكامل

#### W3.4: توحيد واجهات المستخدم

- توحيد 12+ مكوّن لاستخدام `EmptyState` المشترك من `PageLayout`:
  - `SimplifiedProjectCostView.tsx`
  - `TenderPricingWizard.tsx`
  - `TenderStatusCards.tsx`
  - `TenderPricingProcess.tsx` (حالتان)
  - `TenderDetails.tsx`
  - `TechnicalFilesUpload.tsx`
  - `Reports.tsx`
  - `Projects.tsx`
  - `Financial.tsx` (4 بطاقات جانبية)
  - `ProjectCostView.tsx`
  - `ExpenseManagement.tsx`
- استبدال جميع `window.confirm` بـ `AlertDialog` موحدة مع:
  - تصميم متناسق مع التطبيق
  - محتوى وصفي واضح بالعربية
  - أيقونات مناسبة للسياق
  - مثال: `ExpenseManagement.tsx` يستخدم `AlertDialog` للحذف مع أيقونة `Trash2`

#### W3.5: التزامن المركزي للبيانات

- إضافة حقل `isRefreshing` في `FinancialStateContext` لتتبع حالة التحديث
- تحديث `refreshAll()` ليشمل جميع المصادر:
  - الفواتير (Invoices)
  - الموازنات (Budgets)
  - التقارير المالية (Financial Reports)
  - المشاريع (Projects)
  - المنافسات (Tenders)
  - العملاء (Clients)
  - معدلات العملة (Currency Rates)
- بناء مكوّن `LastUpdateIndicator` في `src/components/layout/ui/LastUpdateIndicator.tsx`:
  - عرض وقت آخر تحديث بتنسيق نسبي ("منذ دقيقتين")
  - دعم اللغة العربية عبر `date-fns/locale/arSA`
  - أيقونة متحركة أثناء التحديث (RefreshCw spinner)
  - زر تحديث فوري متاح في الواجهة
  - دعم وضع مُدمج (compact) للمساحات الضيقة
- تكامل `LastUpdateIndicator` في `PageLayout` مع خاصية `showLastUpdate` (افتراضيًا: true)

### 🔧 التحسينات

- إزالة القوائم الصلبة المكررة واستبدالها بمخطط واحد موحد
- تحسين الأداء بـ lazy loading للصفحات
- رسائل واضحة للمستخدم في جميع الحالات الفارغة
- تجربة بصرية موحدة عبر جميع الواجهات
- شفافية كاملة لحالة البيانات مع مؤشر آخر تحديث

### 📝 التوثيق

- تحديث `docs/IMPROVEMENT_ROADMAP.md` لإعلان إكمال المرحلة 3 (100%)
- إنشاء `docs/PHASE3_COMPLETION_REPORT.md` مع تفاصيل كاملة عن الإنجازات
- تعليمات استرداد معالج التسعير في `docs/SECURITY_GUIDE.md` (قيد التحديث)

### 🧪 الاختبار

- جميع الاختبارات تمر بنجاح: **186/186** اختبار في **62** ملف
- معدل النجاح: **100%**
- مدة التنفيذ: 44.45 ثانية
- سيناريو Playwright E2E شامل لمعالج التسعير

### 🛠️ أدوات

- أداة التنظيف `scripts/migrations/cleanup-tender-wizards.ts` لتنقية البيانات التاريخية المسجلة في `app_tender_pricing_wizards`

### 📊 المقاييس

| المؤشر | قبل | بعد | التحسين |
|--------|-----|-----|---------|
| مكونات EmptyState | 0 (12+ نسخة) | 1 (موحد) | -92% تكرار |
| حوارات التأكيد | `window.confirm` | `AlertDialog` | +100% تناسق |
| مؤشرات التحديث | 0 | 1 (شامل) | رؤية كاملة |
| معدل نجاح الاختبار | 99.5% | 100% | +0.5% |

---

## الإصدارات السابقة
