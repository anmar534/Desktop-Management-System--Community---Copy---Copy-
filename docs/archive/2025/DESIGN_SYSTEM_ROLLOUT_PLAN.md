# خطة تنفيذ نظام التصميم الموحد

> **الإصدار:** 1.0.0  
> **تاريخ الإعداد:** 9 أكتوبر 2025  
> **إعداد:** فريق تجربة المستخدم والتطوير الواجهات

هذه الخطة تحدد مراحل وخطوات تطبيق نظام التصميم الموحد على جميع صفحات ومكونات النظام، مع مراعاة أفضل الممارسات في الحوكمة، التطوير التدريجي، وضمان الجودة.

---

## المرحلة 0 – التحضير والحوكمة

| الهدف | بناء إطار الحوكمة وضمان جاهزية الفريق قبل بدء التنفيذ |
|-------|-----------------------------------------------------------|

**حالة التنفيذ:** ✅ تم إعداد حزمة الحوكمة والجاهزية الأولية في `docs/design-system/` (GOVERNANCE.md, ACCEPTANCE_CRITERIA.md, DECISIONS.md, MEASUREMENT_SETUP.md).

1. **تشكيل فريق التنفيذ المتعدد التخصصات**
   - تعيين قائد للتجربة البصرية وقائد تقني.
   - تحديد ممثلين عن الفريق الأمامي، الخلفي، ومديري المنتجات.
2. **تعريف معايير القبول والنجاح**
   - صياغة تعريف «الجاهزية» (Definition of Ready) لكل مكون/صفحة قبل إعادة بنائها.
   - وضع معايير تضمن توافق الـ WCAG والتجاوب وRTL.
3. **تحديث دليل الحوكمة**
   - إنشاء قناة اتصال مخصصة (Slack / Teams) لتوثيق القرارات.
   - توثيق سياسات المراجعة (PR checklist، توافق التصميم).
4. **إعداد بيئة القياس**
   - تفعيل Storybook أو Ladle لتوثيق المكونات المعتمدة.
   - تأكيد وجود أداة تحليل التغطية البصرية (Visual Regression) إن وُجدت.

📦 **المخرجات:** وثيقة حوكمة محدثة، لوحة متابعة (Kanban) للمهام، Storybook مُهيأ.

---

## المرحلة 1 – تدقيق شامل ومواءمة الأرضية المشتركة

| الهدف | حصر الوضع الحالي وتحديد عناصر النظام المرجعي التي يجب تعميمها |
|-------|-----------------------------------------------------------------|

**حالة التنفيذ:** ✅ اكتملت المرحلة 1. تم إصدار `PHASE1_AUDIT_REPORT.md`، وتوثيق الانحرافات اللونية، وإنشاء `COMPONENT_INVENTORY.md` كمرجع للحالة الحالية.

1. **مراجعة التصميم المرجعي**
   - التأكد من اكتمال `docs/DESIGN_SYSTEM.md` وتوافقه مع `tokens.config.ts` و `globals.css`.
2. **إعداد قائمة بالانحرافات**
   - استخراج جميع الألوان والظلال خارج نطاق الـ Tokens باستخدام أداة بحث تلقائية.
   - رصد المكوّنات التي لا تستخدم `PageLayout`, `WidgetContainer`, أو مكونات `ui` القياسية.
3. **تحديد أولويات المكونات الحرجة**
   - تصنيف الانحرافات إلى: أساسية (Layout, Header, Sidebar)، متوسطة (بطاقات، Badges)، منخفضة (صفحات ثانوية).
4. **إنشاء سجل المكونات (Component Inventory)**
   - جدول يوضح كل مكون، حالته (مطابق/غير مطابق)، وخطة المعالجة.

📦 **المخرجات:** تقرير الانحرافات، سجل المكونات، خارطة أولويات التنفيذ.

---

## المرحلة 2 – تحديث البنية التحتية للتصميم

| الهدف | تأمين الأساس الفني لضمان سهولة التبني وإعادة الاستخدام |
|-------|-----------------------------------------------------------|

**حالة التنفيذ:** ✅ اكتملت – تم توحيد مزود السمات، تحديث قصص Storybook، إقفال قواعد lint، وبناء المكتبة المشتركة دون تحذيرات متبقية.

- ✅ إزالة ازدواجية إدارة السمات والاعتماد الكامل على `ThemeProvider` الموحد مع دعم وضع التباين العالي.
- ✅ تحديث قصص `ThemeSwitcher` لقراءة السمات من المزود الفعلي وتوثيق حالات Light/Dark/High Contrast.
- ✅ إعداد قواعد ESLint/Tailwind لمنع استخدام الألوان الثابتة خارج الـ Tokens (تحذيرات مفعّلة على جميع الحالات المخالفة لتسهيل معالجتها تدريجيًا).
- ✅ تنظيف المكونات الأكثر استخدامًا (Clients, Development, Tender Status/Results/Quick Results) من درجات Tailwind الخام واستبدالها بالرموز الدلالية لتقليل التحذيرات النشطة.
- ✅ استخراج المكونات الهيكلية الأساسية (PageLayout, Header, Sidebar) إلى مكتبة `@/components/ui/layout` مع الإبقاء على العناصر الثانوية قيد العمل.
- ✅ توحيد صفحات المالية المحورية (`ExpenseManagement`, `FinancialReports`) على ألوان tokens الدلالية، بما في ذلك البطاقات، الجداول، وحقول الإدخال مع تحديث الحدود إلى `border-border` لضمان دعم السمات.
- ✅ إنشاء وحدة `src/utils/designTokens.ts` لاستهلاك الرموز الدلالية داخل TypeScript (ECharts، MiniCharts) مع دعم قيم alpha والإرجاع الافتراضي.
- ✅ إدخال مكونات `InlineAlert` و`ActionBar` في مكتبة الواجهات وتوثيقها في Storybook لضمان استخدام الرسائل الثابتة وشريط الإجراءات الموحد.
- ✅ تنظيف `FinancialSummaryCard`, `Financial.tsx`, `MonthlyExpensesChart`, و`TenderPricingProcess` من الدرجات الخام واعتماد الأنماط الدلالية (بما في ذلك الأزرار، المؤشرات، أدوات الرسم البياني).
- ✅ توحيد صفحة `Invoices` بسيناريوهاتها التحليلية (إحصاءات، بطاقات تفصيل، حالات الدفع) على الدرجات الدلالية والتدرجات الموحدة، وإزالة 33 تحذيرًا من قاعدة lint.
- ✅ إعادة تصميم `PricingSummary` وواجهة `EnhancedProjectDetails` (الحالات الفارغة + مخطط الخط الزمني) على الرموز الدلالية بالكامل، ليصل عدد تحذيرات lint المتبقية إلى 63 فقط قبل معالجة شاشات إنشاء الكيانات.
- ✅ معالجة شاشة `NewBankAccount` مع استبدال التدرجات، الحدود، وألوان حالة الحساب بتوكينات دلالية، وخفض عدد تحذيرات lint النشطة إلى 57 مع بقاء العمل على نماذج الإنشاء الأخرى.
- ✅ تحويل شاشة `NewBudget` بالكامل إلى التوكينات الدلالية (التدرجات، أزرار الحذف، إشعارات التجاوز) ما نتج عنه انخفاض إضافي في تحذيرات lint إلى 51 وتحضير الأرضية لشاشات `NewInvoice` و`NewReport`.
- ✅ استبدال جميع التدرجات والحدود وأزرار الإزالة في `NewInvoice` بالتوكينات الدلالية، مما قلّص التحذيرات النشطة إلى 48 وترك `NewReport` كنموذج الإنشاء الأخير الذي يعتمد درجات Tailwind الخام.
- ✅ استكمال تحويل `NewReport` إلى التوكينات الدلالية (التدرجات والحدود)، لتصبح شاشات نماذج الإنشاء الثلاثة متوافقة بالكامل وينخفض العدّ المتبقي إلى 46 تحذيرًا.
- ✅ مواءمة `RemindersCard` مع التوكينات الدلالية (التدرج، ألوان الأيقونات) لتخفيض التحذيرات إلى 45 استعدادًا لحزمة التقارير والودجات.
- ✅ تحديث صفحة `Reports` الرئيسية لاستخدام التدرجات الدلالية في ترويسة `PageLayout`، مما خفّض العد المتبقي إلى 43 تحذيرًا.
- ✅ تنظيف `TechnicalFilesUpload` من الدرجات الرمادية/الحمراء الثابتة (منطقة الإسقاط، بطاقات الملفات، الأزرار) وخفض العدد المتبقي إلى 35 تحذيرًا يتركز على الودجات والـ dialogs.
- ✅ استبدال شارة الحالات المخصصة في `CalendarWidget` بمكون `StatusBadge` المعمم وضبط ألوان التقويم، لتصبح جميع ودجات التذكيرات/التقويم متوافقة ويهبط العد المتبقي إلى 34 تحذيرًا.
- ✅ توحيد ودجات الإجراءات والزمن (`QuickActions`, `TimelineWidget`) على التوكينات الدلالية للحدود والنصوص والرموز المختصرة، ليصل العدد الحالي إلى 32 تحذيرًا معظمها في مكونات الحوار وملفات البيانات.
- ✅ اعتماد طبقة التعتيم في `alert-dialog` و`dialog` على `bg-background/80` مع دعم الـ blur بدلًا من `bg-black/50` لضمان التوافق مع السمات وتقليل التحذيرات إلى 30 مع بقاء العمل على ملفات البيانات و`TenderPricingWizard`.
- ✅ إعادة هيكلة بيانات `expenseCategories` لاستخدام فئات التوكينات الدلالية في التكرار وحالات الدفع، مع ربط الألوان التحليلية بـ `designTokens`.
- ✅ إزالة تحذيرات lint الأخيرة عبر Typed mocks لاختبارات المقاييس وتحديث خدمة أسعار الصرف، ليصبح العدّ المتبقي صفرًا مع تفعيل قواعد `no-explicit-any` بالكامل.

1. **مواءمة إدارة السمات**
   - إزالة ازدواجية إدارة الوضع الداكن من `AppShell` والاعتماد الكامل على `ThemeProvider`.
   - إضافة دعم صريح لـ High Contrast في مكونات التحكم.
2. **تحديث الأدوات والتكوينات**
   - إضافة قواعد ESLint/Tailwind تمنع استخدام الألوان الثابتة خارج الـ Tokens.
   - توفير وظائف مساعدة لقراءة الألوان داخل TypeScript حيث يلزم.
3. **بناء مكتبة مكونات أساسية**
   - استخراج المكونات المشتركة (Header, Sidebar, PageHeader) إلى مكتبة `@/components/ui/layout`.
   - إنشاء مكونات هوية (StatusBadge, InlineAlert, ActionBar) مبنية على التصميم الموحد.
4. **إعداد أمثلة Storybook**
   - توثيق حالات المكونات الأساسية الثلاث (Light/Dark/High Contrast) لضمان الاتساق البصري.

📦 **المخرجات:** مكتبة مكونات أساسية محدثة، قواعد lint جديدة، قصص Storybook.

---

## المرحلة 3 – إعادة بناء المكونات المشتركة

| الهدف | ضمان أن المكونات الهيكلية الرئيسية متوافقة بالكامل قبل الانتقال للصفحات |
|-------|------------------------------------------------------------------------|

**حالة التنفيذ:** ✅ اكتملت – تم تحديث PageLayout وHeader وSidebar على التوكينات الدلالية، وتوحيد جميع البطاقات/الرسائل المشتركة على StatusBadge وInlineAlert مع تمرير اختبارات lint.

1. **تحديث `PageLayout`**
   - استبدال الألوان الثابتة بـ `bg-background`, `bg-card`, `text-muted-foreground`.
   - ضمان ظهور زر الرجوع تلقائيًا عند توفر `onBack`.
   - إضافة دعم `prefers-reduced-motion` للحركات.

2. **ضبط `Header` و`Sidebar`**
   - توحيد الألوان باستخدام `card`, `muted`, `accent`.
   - إعادة ترتيب أزرار الإجراءات لتتبع النمط الرسمي (زر الإنشاء على اليسار، البقية متجانسة).
3. **تحديث مكونات البطاقات/الرسائل**
   - تطبيق `StatusBadge`, `InlineAlert` على `TenderCard`, `ProjectCard`, وغيرها.
   - نموذج تطبيقي: `TenderStatusCards` الآن يستخدم `StatusBadge` و`InlineAlert` لإبراز حالات المنافسات والتنبيهات.
   - تم تعميم `ProjectCard` في `Projects` و`ReportCard` في `Reports` على `StatusBadge` مع تحذيرات التكلفة عبر `InlineAlert` لضمان اتساق الواجهات التحليلية.
   - تم توحيد `AnnualKPICards` على `StatusBadge` مع ألوان الحالة الدلالية وإشعارات التباين لضمان تجربة قياس موحدة عبر بطاقات الأداء.
   - تم مواءمة `FinancialSummaryCard` لعرض الشارات عبر `StatusBadge` وربط تحذيرات التدفق النقدي بـ `InlineAlert` لتثبيت الأنماط التحليلية الأساسية.
   - تم ترقية `TenderCard` إلى منظومة التوكينات (شارات الحالة، التنبيه عند انتهاء الموعد، تنبيه الملفات الفنية) مع تحديث `useTenderStatus` لإرجاع نغمات موحدة.
   - تم تحديث `Clients` لعرض حالة العلاقة/النوع/السداد عبر `StatusBadge` مع تنبيه متابعة تلقائي لمخاطر التحصيل.
   - تعميم استخدام `WidgetContainer` أو نمط مشابه للبطاقات.
4. **اختبار الانحدار البصري**
   - التقاط لقطات قبل/بعد لضمان عدم تغير الوظائف أو الـ UX سلبًا.

📦 **المخرجات:** مكونات مشتركة متوافقة، نتائج اختبارات بصرية مثبتة، توثيق تغييرات التبعية.

---

## المرحلة 4 – ترحيل الصفحات حسب الأولوية

| الهدف | تحديث الصفحات تدريجيًا لضمان الاستقرار والسرعة |
|-------|-------------------------------------------------|

**حالة التنفيذ:** 🚧 قيد التنفيذ – اكتملت تحويلات الشارات والتنبيهات في صفحات `Projects`, `Tenders`, `Clients`، كما تم توحيد حالات الجداول في `AdvancedDashboard` على `StatusBadge`.

1. **موجة 1 – الصفحات عالية التكرار**
   - لوحة التحكم (`AdvancedDashboard`)، المشاريع (`Projects`)، المنافسات (`Tenders`).
   - ✅ توحيد رؤوس الصفحات والبطاقات على `StatusBadge` و`InlineAlert`.
   - ✅ استكمال دمج التوكينات الدلالية في جداول المشاريع والفواتير داخل لوحة التحكم.
   - ✅ ترقية شاشة `Invoices` لاستخدام `StatusBadge` في الحالات الرئيسية مع تنبيه تحصيلي موحد عبر `InlineAlert`.
   - ✅ مواءمة صفحة `Financial` مع نظام الشارات والتنبيهات المعمم (الموردون، المعاملات، الفواتير، الموازنات، المشاريع، المنافسات، التقارير) وإضافة تنبيه تحليلي ديناميكي عبر `InlineAlert`.
   - ⏳ متابعة مراقبة مؤشرات الأداء المصاحبة بعد النشر.
2. **موجة 2 – نماذج الإدخال المعقدة**
   - `NewProjectForm`, `NewTenderForm`, `TenderPricingWizard`.
    - ✅ مواءمة `ExpenseManagement` مع `StatusBadge` و`InlineAlert` في جداول المصروفات والتنبيهات التشغيلية.
    - ✅ تحديث `FinancialReports` لعرض حالات الإنتاج عبر `StatusBadge` وتنبيهات حالات الفشل/التوليد.
   - ✅ توحيد `NewProjectForm` لشارات نوع العميل وتعقيد المشروع باستخدام `StatusBadge` مع تنبيه تقديري ديناميكي.
   - ✅ تحويل `NewTenderForm` لتحليل الموعد النهائي والقيمة عبر `StatusBadge` و`InlineAlert` وتحذير موجز.
   - ✅ ترقية `TenderPricingWizard` لإظهار حالة الحفظ والتدقيق الفني/المالي من خلال `StatusBadge` مع خرائط نغمات موحدة.
3. **موجة 3 – الصفحات الثانوية والتقارير**
   - التقارير المالية، الإعدادات، صفحات إدارة الحسابات.
   - مراجعة تخطيطات الجداول والبطاقات.
   - ✅ إعادة بناء `TechnicalFilesUpload` لتنبيه اختيار المنافسة وشارة حالة الرفع عبر `InlineAlert` و`StatusBadge`.
   - ✅ توحيد `Budgets` على `StatusBadge` مع تلخيص ديناميكي للحالة عبر `InlineAlert` لتحسين وضوح مؤشرات الإنفاق.
   - ✅ تحديث `Settings` لإظهار بيانات النظام بختم `StatusBadge` ورسائل معالجة الملفات عبر `InlineAlert` بدل الشارات والتنبيهات المخصصة.
4. **لكل موجة**
   - تنفيذ تحليل UX سريع (Heuristic Review) والتأكد من دعم RTL والتباين.
   - اختبار وظيفي (Smoke Tests) + فحص لقطات VisReg.

📦 **المخرجات:** صفحات محدثة وفق الأولويات، تقارير مراجعة UX، سجل نتائج الاختبارات.

---

## المرحلة 5 – ضمان الجودة والتوثيق

| الهدف | تثبيت النتائج وضمان عدم التراجع |
|-------|-----------------------------------|

1. **إجراء اختبارات شاملة**
   - Vitest للوحدات، Playwright للواجهات، ولمن يلزم Electron smoke tests.
2. **تحديث الوثائق**
   - توسيع `DESIGN_SYSTEM.md` بجدول «قبل/بعد» وأمثلة تعليمية.
   - إعداد دليل «كيفية الالتزام بالتصميم الموحد» للمطورين.
3. **نشر دليل مراجعة PR**
   - قائمة تحقق تشمل: استخدام tokens، التباين، التوافق مع السمات، دعم RTL.
4. **بناء لوحة مراقبة**
   - Dashboard في Storybook أو Documentation site يعرض حالة التوافق لكل صفحة.

📦 **المخرجات:** تغطية اختبارات محدثة، وثائق موسعة، دليل مراجعة PR، لوحة مراقبة التوافق.

---

## المرحلة 6 – التحسين المستمر والمراقبة

| الهدف | منع التراجع وضمان تبني الفريق للهوية الجديدة |
|-------|------------------------------------------------|

1. **إطلاق نسخة داخلية (Beta)**
   - جمع ملاحظات المستخدمين النهائيين وفرق الدعم.
2. **مراقبة مؤشرات الأداء**
   - تتبع مؤشرات مثل سرعة التطوير، وقت مراجعة PR، شكاوى UX.
3. **جلسات تدريب شهرية**
   - ورش عمل قصيرة لعرض حالات الاستخدام الجيدة وتبادل الخبرات.
4. **جدولة مراجعات دورية**
   - مراجعة ربع سنوية للنظام لضمان تحديث الـ Tokens ومواكبة المتطلبات.

📦 **المخرجات:** تقارير تغذية راجعة، مؤشرات أداء مُحدثة، خطة تدريب، جدول مراجعات.

---

## الجدول الزمني المقترح

| المرحلة | المدة التقديرية | نقاط التعليق |
|---------|-----------------|----------------|
| 0 | أسبوع | تجهيز الفريق والحوكمة |
| 1 | أسبوعان | يكتمل بعد تسليم تقرير الانحرافات |
| 2 | 2-3 أسابيع | يعتمد على حجم refactoring للمكونات الأساسية |
| 3 | 2 أسابيع | يمكن تنفيذها متوازية جزئيًا |
| 4 | 4-6 أسابيع | تقسيم الموجات وتداخلها بناءً على الأولوية |
| 5 | أسبوع | اختبار نهائي وتوثيق |
| 6 | مستمر | مراقبة وتحسين |

> **ملاحظة:** يمكن تكييف الجدول بناءً على توافر الفريق وحجم التعديلات المطلوبة في كل صفحة.

---

## عوامل النجاح الحرجة

- الالتزام الصارم باستخدام الـ Tokens والمكونات الموحدة في جميع التعديلات الجديدة.
- وجود حوكمة نشطة ومراجعة مشتركة بين فرق التصميم والتطوير.
- تطبيق اختبارات بصرية ووظيفية بعد كل دفعة تحديث.
- توثيق مستمر وتدريب للفريق لمنع العودة إلى الأنماط القديمة.

---

## المتابعة والقياس

- **لوحة قياس (Dashboard):** نسبة الصفحات الملتزمة، عدد الانحرافات المفتوحة، وقت معالجة الانحراف.
- **مؤشرات تجربة المستخدم:** معدل رضا المستخدمين الداخليين، انخفاض الشكاوى عن التباين أو التعارض البصري.
- **مؤشرات الفريق:** سرعة تطوير المكونات، زمن مراجعة PR، نسبة إعادة العمل.

---

باتباع هذه الخطة المتسلسلة، يمكن توحيد الهوية البصرية للنظام تدريجيًا دون التأثير على استقرار المنصة، مع ضمان تبني الفريق للممارسات الموحدة وتعظيم الفائدة من نظام التصميم المركزي.

---

## خطة إدارة المخاطر

| الخطر | الاحتمالية | الأثر | خطة التخفيف |
|-------|-------------|-------|--------------|
| تأخير تسليم المكونات الأساسية | متوسطة | عالية | وضع جدول زمني مفصل لكل مكون، ومراجعة أسبوعية للتقدم مع القائد التقني |
| مقاومة الفرق لاعتماد المكونات المعيارية | متوسطة | متوسطة | جلسات تعريفية، مشاركة قصص نجاح، اعتماد سياسة «لا مكوّن جديد خارج المكتبة» |
| تعارضات مرئية في وضع التباين العالي | منخفضة | عالية | اختبار يدوي لكل مكوّن جديد في Light/Dark/High Contrast قبل الدمج |
| تضارب مع الالتزامات التطويرية الأخرى | عالية | متوسطة | مزامنة شهرية مع مديري المنتجات وتحديد أولويات مشتركة في لوحة Kanban |
| تراجع الأداء نتيجة تغييرات CSS واسعة | منخفضة | متوسطة | مراقبة مؤشرات الأداء بعد كل دفعة ونشر Hotfix عند الحاجة |

### آلية المراقبة

- اجتماع مخاطر ربع شهري لتحديث السجل ومراجعة خطط التخفيف.
- توثيق كل حادثة أو انحراف في سجل خاص وربطها بـ PR لمعالجة الجذور.

---

## خطة التواصل والتدريب

1. **لوحة متابعة أسبوعية**
   - إرسال تحديث مختصر عبر البريد أو Slack يلخص التقدم، المعوقات، والخطوات القادمة.
2. **جلسات عرض شهرية**
   - استعراض المكونات المحدثة في Storybook مع المطورين ومصممي المنتجات.
3. **ورش عمل تفاعلية**
   - تدريب الفرق على استخدام مكتبة `@/components/ui/layout` وشرح حالات الاستخدام النموذجية.
4. **مكتبة معرفية حية**
   - إضافة أمثلة «كيف تفعل» في `docs/design-system/tutorials/` مع كود جاهز للنسخ.
5. **قناة دعم فوري**
   - تخصيص قناة دعم يومية للرد على الاستفسارات التقنية أو التصميمية خلال ساعات محددة.

> **مؤشر نجاح الاتصال:** انخفاض الأسئلة المتكررة حول الألوان والخطوط بنسبة 50٪ خلال أول ثلاثة أشهر.

---

## مصفوفة التتبع والتنفيذ

| العنصر | المالك | حالة التنفيذ | تاريخ آخر تحديث | ملاحظات |
|--------|--------|---------------|------------------|----------|
| تحديث `PageLayout` | فريق الواجهات | قيد التنفيذ | 10 أكتوبر 2025 | ينتظر مراجعة التباين |
| اعتماد `StatusBadge` | فريق تجربة المستخدم | مكتمل | 8 أكتوبر 2025 | متاح في Storybook |
| تنظيف صفحات المالية | فريق المالية الرقمية | جاري المتابعة | 10 أكتوبر 2025 | 82٪ من التحذيرات أزيلت؛ Financial + Invoices + PricingSummary + NewBankAccount + NewBudget + NewInvoice + NewReport مكتملة |
| تنظيف ودجات لوحة التحكم | فريق لوحة التحكم | قيد البدء | 10 أكتوبر 2025 | RemindersCard مكتملة؛ CalendarWidget + QuickActions + TimelineWidget قيد المعالجة |
| إعداد دليل PR | قائد الحوكمة | مجدول | 12 أكتوبر 2025 | الصيغة الأولية قيد المراجعة |
| تشغيل اختبارات VisReg | فريق الاختبار | غير مفعّل | — | بانتظار إعداد البيئة |

> **تنويه:** يجب تحديث هذه المصفوفة أسبوعيًا وربطها بلوحة Kanban لضمان التناسق بين الوثائق والخطة التنفيذية.

---

## ملحق أ – قائمة المكوّنات ذات الأولوية القصوى

| المكوّن | الفريق المسؤول | سبب الأولوية | متطلبات ما قبل البدء |
|---------|----------------|---------------|-----------------------|
| `TenderPricingProcess` | فريق المناقصات | أكثر الملفات توليدًا لتحذيرات قبل التنظيف | تأكيد جاهزية بيانات الاختبار |
| `FinancialSummaryCard` | فريق المالية الرقمية | يظهر في معظم التقارير التنفيذية | توثيق حالات Light/Dark في Storybook |
| `EnhancedProjectDetails` | فريق المشاريع | يحتوي على جداول معقدة وشرائط تقدم | تحديث مخطط البيانات وخيارات العرض |
| `TimelineWidget` | فريق لوحة التحكم | محور في الصفحة الرئيسية للمديرين | اختبار لقطات VisReg قبل وبعد |
| `QuickActions` | فريق العمليات | تكرار يومي للمستخدمين | مطابقة النصوص مع دليل المحتوى |

> **إرشاد:** يجب عدم بدء العمل على أي مكوّن قبل التأكد من اكتمال متطلبات ما قبل البدء لتجنب إعادة العمل.

---

## ملحق ب – خارطة طريق تحسين مؤشرات الأداء

1. **الربع الرابع 2025**
   - رفع نسبة الصفحات الملتزمة بالتصميم الموحد إلى 55٪.
   - تقليل زمن مراجعة PR المتعلق بالتصميم إلى أقل من يومين عمل.
2. **الربع الأول 2026**
   - الوصول إلى 75٪ التزام عبر اللوحات الأساسية والنماذج المعقدة.
   - إدخال اختبارات Visual Regression في ممر النشر المؤتمت.
3. **الربع الثاني 2026**
   - توثيق 100٪ من المكونات المشتركة في Storybook مع حالات تفاعلية.
   - قياس رضا الفرق الداخلية عبر استبيان مخصص وتحقيق معدل 4 من 5 كحد أدنى.

---

## الملحق ج – مرجع الأدوات والموارد

- `docs/design-system/GOVERNANCE.md`: قواعد اتخاذ القرار ومصفوفات المسؤوليات.
- `docs/design-system/TOKENS_REFERENCE.md`: قائمة الرموز الدلالية وتدرجاتها المعتمدة.
- `storybook-static/`: أحدث نسخة منشورة من Storybook للاطلاع على المكوّنات.
- `scripts/audit-design-tokens.ts`: أداة مراقبة الانحرافات اللونية وتوليد التقارير الدورية.
- `playwright.desktop.config.ts`: تهيئة الاختبارات البصرية (يمكن توسيعها لدعم مقارنة السمات).

> **نصيحة تشغيلية:** تأكد من مواءمة أي أدوات جديدة مع هذه القائمة وتحديث المرجع فورًا لتفادي تضارب الإصدارات.
