# دليل التثبيت - نظام إدارة شركة المقاولات

## 📥 تحميل التطبيق

### من GitHub Releases

1. انتقل إلى صفحة [Releases](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/releases)
2. حمّل أحدث إصدار: `DesktopManagementSystem-x.x.x-win-x64.exe`

### التحقق من سلامة الملف (اختياري)

```powershell
# احسب SHA256 للملف المحمل
Get-FileHash -Path ".\DesktopManagementSystem-x.x.x-win-x64.exe" -Algorithm SHA256

# قارنه بالقيمة في checksums.txt
```

## 💻 متطلبات النظام

### الحد الأدنى

- **نظام التشغيل**: Windows 10 (64-bit) أو أحدث
- **المعالج**: Intel Core i3 أو مكافئ
- **الذاكرة**: 4GB RAM
- **التخزين**: 500MB مساحة حرة
- **الشاشة**: 1366×768 أو أعلى

### المُوصى به

- **نظام التشغيل**: Windows 11 (64-bit)
- **المعالج**: Intel Core i5 أو أعلى
- **الذاكرة**: 8GB RAM
- **التخزين**: 2GB مساحة حرة (للبيانات والنسخ الاحتياطية)
- **الشاشة**: 1920×1080 أو أعلى

## 🚀 التثبيت

### الطريقة الأولى: التثبيت القياسي

1. **تشغيل ملف التنصيب**

   ```
   انقر مرتين على: DesktopManagementSystem-x.x.x-win-x64.exe
   ```

2. **اختيار اللغة**

   - اختر العربية من القائمة المنسدلة

3. **قراءة اتفاقية الترخيص**

   - اقرأ الاتفاقية ووافق عليها

4. **اختيار موقع التثبيت**

   - الموقع الافتراضي:
     ```
     C:\Users\<YourName>\AppData\Local\Programs\desktop-management-system-community
     ```
   - يمكنك تغييره إذا أردت

5. **خيارات إضافية**

   - ✅ إنشاء اختصار على سطح المكتب
   - ✅ إنشاء اختصار في قائمة البدء
   - ✅ تشغيل التطبيق بعد التثبيت

6. **إكمال التثبيت**
   - انقر على "تثبيت" وانتظر حتى يكتمل

### الطريقة الثانية: التثبيت الصامت (للمسؤولين)

```powershell
# تثبيت صامت مع الخيارات الافتراضية
.\DesktopManagementSystem-x.x.x-win-x64.exe /S

# تثبيت صامت في موقع محدد
.\DesktopManagementSystem-x.x.x-win-x64.exe /S /D=C:\MyApps\DesktopManagement
```

## 🔄 التحديث

### التحديث التلقائي (مُوصى به)

1. التطبيق يتحقق من التحديثات تلقائياً كل 6 ساعات
2. عند توفر تحديث، ستظهر رسالة إشعار
3. اختر "تحميل" لتنزيل التحديث في الخلفية
4. عند اكتمال التحميل، اختر "تثبيت وإعادة التشغيل"
5. البيانات الخاصة بك محمية تلقائياً

### التحديث اليدوي

1. حمّل آخر إصدار من GitHub
2. شغّل ملف التنصيب الجديد
3. سيتم استبدال الإصدار القديم تلقائياً
4. **مهم**: لا تحذف التطبيق القديم أولاً - فقط ثبّت فوقه

## 📂 مواقع الملفات

### ملفات التطبيق

```
C:\Users\<YourName>\AppData\Local\Programs\desktop-management-system-community\
```

### بيانات المستخدم (مهم جداً - لا تحذف)

```
C:\Users\<YourName>\AppData\Roaming\desktop-management-system-community\
```

تحتوي على:

- `config.json` - الإعدادات
- `databases/` - قواعد البيانات
- `backups/` - النسخ الاحتياطية
- `error-logs/` - سجلات الأخطاء

### النسخ الاحتياطية التلقائية

```
C:\Users\<YourName>\AppData\Roaming\desktop-management-system-community\backups\
```

## 🔧 إعداد أول استخدام

### 1. تشغيل التطبيق لأول مرة

```
Start Menu > نظام إدارة شركة المقاولات
```

### 2. التهيئة الأولية

- سيتم إنشاء قاعدة البيانات تلقائياً
- سيتم إنشاء مجلدات البيانات
- يمكنك البدء مباشرة

### 3. استيراد بيانات موجودة (اختياري)

```
الإعدادات > البيانات > استيراد
```

## 🛡️ الأمان والخصوصية

### تخزين البيانات

- جميع البيانات محلية على جهازك
- لا يتم إرسال أي بيانات للخوادم
- البيانات الحساسة مشفرة

### الأذونات المطلوبة

- **قراءة/كتابة الملفات**: لحفظ البيانات
- **الوصول للإنترنت**: للتحديثات فقط
- **Windows Credential Manager**: لتخزين مفاتيح التشفير بأمان

## ❌ إلغاء التثبيت

### الطريقة الأولى: من لوحة التحكم

```
Settings > Apps > Apps & features
ابحث عن: نظام إدارة شركة المقاولات
انقر > Uninstall
```

### الطريقة الثانية: من المثبت

```
C:\Users\<YourName>\AppData\Local\Programs\desktop-management-system-community\Uninstall.exe
```

### ⚠️ حفظ البيانات

عند إلغاء التثبيت، سيُسأل:

- **حذف جميع البيانات**: يحذف كل شيء (لا يمكن التراجع)
- **الحفاظ على البيانات**: يبقي بياناتك لإعادة التثبيت لاحقاً

### نسخ احتياطي قبل إلغاء التثبيت

```powershell
# انسخ مجلد البيانات إلى مكان آمن
Copy-Item -Path "$env:APPDATA\desktop-management-system-community" -Destination "D:\Backup\AppData" -Recurse
```

## 🔍 حل المشاكل

### التطبيق لا يبدأ

1. تحقق من متطلبات النظام
2. شغّل كـ Administrator
3. أعد تثبيت التطبيق
4. تحقق من سجلات الأخطاء:
   ```
   %APPDATA%\desktop-management-system-community\error-logs\
   ```

### التحديث التلقائي لا يعمل

1. تحقق من اتصال الإنترنت
2. تحقق من الإعدادات:
   ```
   الإعدادات > التحديثات > تفعيل التحديثات التلقائية
   ```
3. تحديث يدوي

### فقدان البيانات

1. تحقق من النسخ الاحتياطية:
   ```
   %APPDATA%\desktop-management-system-community\backups\
   ```
2. استعادة من نسخة احتياطية:
   ```
   الإعدادات > البيانات > استعادة
   ```

### رسالة "التطبيق قيد التشغيل"

- يمكن تشغيل نسخة واحدة فقط
- تحقق من Task Manager
- أنهي العملية القديمة وشغّل من جديد

## 📞 الدعم الفني

### الحصول على المساعدة

1. **التوثيق**: [دليل المستخدم](./USER_GUIDE_ARABIC.md)
2. **GitHub Issues**: [فتح تذكرة دعم](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/issues)
3. **إرسال تقرير خطأ**: من داخل التطبيق
   ```
   Help > Send Error Report
   ```

### قبل طلب المساعدة، جهّز:

- رقم الإصدار: `Help > About`
- نظام التشغيل والإصدار
- وصف المشكلة بالتفصيل
- سجلات الأخطاء (إن وجدت)

## 📋 قائمة التحقق

### بعد التثبيت

- [ ] التطبيق يعمل بنجاح
- [ ] البيانات محفوظة في الموقع الصحيح
- [ ] النسخ الاحتياطي التلقائي يعمل
- [ ] التحديثات التلقائية مفعّلة

### قبل إلغاء التثبيت

- [ ] نسخة احتياطية للبيانات
- [ ] تصدير التقارير المهمة
- [ ] حفظ الإعدادات

## 🎯 نصائح مهمة

1. **احفظ نسخ احتياطية دورية**

   ```
   الإعدادات > البيانات > نسخ احتياطي
   ```

2. **لا تحذف مجلد AppData يدوياً**

   - استخدم خيار إلغاء التثبيت فقط

3. **فعّل التحديثات التلقائية**

   - تحصل على آخر التحسينات والإصلاحات

4. **راقب سجلات الأخطاء**
   - يتم إرسالها تلقائياً للمطورين
   - تساعد في تحسين التطبيق

---

**آخر تحديث**: أكتوبر 2025  
**الإصدار**: 1.0.0
