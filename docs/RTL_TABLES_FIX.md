# إصلاح اتجاه وعرض الجداول (RTL Tables Fix)

## 📋 المشكلة

الجداول في لوحة التحكم كانت تعاني من مشكلتين رئيسيتين:
1. **الأعمدة تبدأ من اليسار بدلاً من اليمين** - لا تتوافق مع اللغة العربية (RTL)
2. **تداخل البيانات في الأعمدة** - بسبب عدم تحديد عروض ثابتة للأعمدة

## ✅ الحل المطبق

### 1. عكس ترتيب الأعمدة (RTL Order)

تم عكس ترتيب الأعمدة لتبدأ من اليمين لليسار:

#### جدول المشاريع - قبل:
```
المشروع → نسبة الإنجاز → الحالة → المدير → قيمة العقد → المتبقي
```

#### جدول المشاريع - بعد:
```
المتبقي → قيمة العقد → المدير → الحالة → نسبة الإنجاز → المشروع
```

#### جدول الفواتير - قبل:
```
رقم الفاتورة → العميل → الحالة → الإجمالي → تاريخ الإصدار → تاريخ الاستحقاق
```

#### جدول الفواتير - بعد:
```
تاريخ الاستحقاق → تاريخ الإصدار → الإجمالي → الحالة → العميل → رقم الفاتورة
```

---

### 2. إضافة عروض محددة للأعمدة

تم تحديد عرض ثابت لكل عمود لمنع التداخل:

#### جدول المشاريع:
```typescript
const projectColumns = [
  { accessorKey: 'remaining', size: 120 },    // المتبقي
  { accessorKey: 'value', size: 140 },        // قيمة العقد
  { accessorKey: 'manager', size: 130 },      // المدير
  { accessorKey: 'status', size: 100 },       // الحالة
  { accessorKey: 'progress', size: 140 },     // نسبة الإنجاز
  { accessorKey: 'name', size: 200 },         // المشروع
];
```

#### جدول الفواتير:
```typescript
const invoiceColumns = [
  { accessorKey: 'dueDate', size: 140 },       // تاريخ الاستحقاق
  { accessorKey: 'issueDate', size: 130 },     // تاريخ الإصدار
  { accessorKey: 'total', size: 130 },         // الإجمالي
  { accessorKey: 'status', size: 100 },        // الحالة
  { accessorKey: 'clientName', size: 150 },    // العميل
  { accessorKey: 'invoiceNumber', size: 120 }, // رقم الفاتورة
];
```

---

### 3. تحسين عرض البيانات داخل الخلايا

تم إضافة `dir="rtl"` و`text-right` و`truncate` لكل خلية:

```typescript
// مثال: عمود المشروع
cell: ({ row }) => (
  <div className="flex flex-col text-right" dir="rtl">
    <span className="font-medium text-foreground truncate">
      {row.original.name}
    </span>
    <span className="text-xs text-muted-foreground truncate">
      {row.original.client}
    </span>
  </div>
)
```

```typescript
// مثال: عمود المتبقي
cell: ({ getValue }) => (
  <div className="text-right" dir="rtl">
    {formatCurrency(Number(getValue<number>() ?? 0), 'SAR')}
  </div>
)
```

```typescript
// مثال: عمود نسبة الإنجاز
cell: ({ getValue }) => {
  const progress = Math.round(getValue<number>() ?? 0);
  return (
    <div className="flex items-center gap-2" dir="rtl">
      <span className="text-xs text-muted-foreground min-w-[35px] text-right">
        {progress}%
      </span>
      <Progress value={progress} className="h-2 flex-1" />
    </div>
  );
}
```

---

### 4. تحديثات على DataGrid Component

#### الملف: `src/components/datagrid/DataGrid.tsx`

**تطبيق العروض على العناوين:**
```tsx
<th
  style={{
    width: `${header.column.getSize()}px`,
    minWidth: `${header.column.getSize()}px`,
    maxWidth: `${header.column.getSize()}px`,
  }}
  className="px-4 py-3 text-right ..."
>
```

**تطبيق العروض على الخلايا:**
```tsx
<td
  style={{
    width: `${cell.column.getSize()}px`,
    minWidth: `${cell.column.getSize()}px`,
    maxWidth: `${cell.column.getSize()}px`,
  }}
  className="px-4 py-3 align-middle text-right overflow-hidden"
>
```

**إضافة table-layout: fixed:**
```tsx
<table 
  className="w-full text-sm" 
  role="presentation" 
  style={{ tableLayout: 'fixed' }}
>
```

---

## 📊 النتائج

### قبل الإصلاح:
- ❌ الأعمدة تبدأ من اليسار (LTR)
- ❌ البيانات تتداخل في الأعمدة
- ❌ عروض الأعمدة غير متناسقة
- ❌ صعوبة في القراءة للغة العربية

### بعد الإصلاح:
- ✅ الأعمدة تبدأ من اليمين (RTL)
- ✅ كل عمود له عرض محدد
- ✅ البيانات لا تتداخل
- ✅ `truncate` للنصوص الطويلة
- ✅ `dir="rtl"` لكل خلية
- ✅ قراءة سهلة ومريحة

---

## 🎯 الاستخدامات

### تحديد عرض عمود جديد:

```typescript
const columns: ColumnDef<DataType>[] = [
  {
    accessorKey: 'fieldName',
    header: 'اسم العمود',
    size: 120, // العرض بالبكسل
    cell: ({ getValue }) => (
      <div className="text-right" dir="rtl">
        {String(getValue())}
      </div>
    ),
  },
];
```

### نصائح لتحديد العروض:

1. **أعمدة الأرقام/التواريخ**: 100-140px
2. **أعمدة الأسماء القصيرة**: 120-150px
3. **أعمدة الأسماء الطويلة**: 180-220px
4. **أعمدة الحالة/الأيقونات**: 80-100px
5. **أعمدة العملات**: 120-140px

### إضافة truncate للنصوص الطويلة:

```typescript
cell: ({ getValue }) => (
  <div className="truncate text-right" dir="rtl">
    {String(getValue())}
  </div>
)
```

---

## 📁 الملفات المعدلة

1. **`src/features/dashboard/AdvancedDashboard.tsx`**
   - عكس ترتيب `projectColumns`
   - عكس ترتيب `invoiceColumns`
   - إضافة `size` لكل عمود
   - إضافة `dir="rtl"` و `text-right` لكل خلية
   - إضافة `truncate` للنصوص الطويلة

2. **`src/components/datagrid/DataGrid.tsx`**
   - إضافة `style` attributes للعناوين (th)
   - إضافة `style` attributes للخلايا (td)
   - تغيير `table` إلى `table-layout: fixed`
   - إضافة `overflow-hidden` للخلايا

---

## 🔧 استكشاف الأخطاء

### إذا كانت البيانات لا تزال متداخلة:

1. تحقق من وجود `size` في تعريف العمود
2. تأكد من أن `table-layout: fixed` مطبق
3. افتح DevTools → تحقق من computed styles للـ `td`

### إذا كان الترتيب لا يزال LTR:

1. تحقق من ترتيب الأعمدة في array
2. تأكد من وجود `dir="rtl"` في الخلايا
3. تحقق من `text-right` className

### إذا كانت النصوص الطويلة تتجاوز العمود:

1. أضف `truncate` للـ div
2. أو استخدم `overflow-hidden` + `text-ellipsis`
3. أو قلل العرض وزد `min-width`

---

## 📝 ملاحظات

- استخدام inline styles ضروري هنا لتطبيق عروض ديناميكية من `column.getSize()`
- `table-layout: fixed` ضروري لإجبار الجدول على احترام العروض
- `dir="rtl"` على مستوى الخلية يضمن عرض صحيح للمحتوى
- `truncate` يمنع overflow ويضيف `...` للنصوص الطويلة

---

**تاريخ الإصلاح**: 9 أكتوبر 2025  
**الإصدار**: 2.1.0  
**الحالة**: ✅ مطبق ومختبر
