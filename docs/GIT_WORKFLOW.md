# استراتيجية Git Flow لمشروع Desktop Management System

**تاريخ الإنشاء:** 13 أكتوبر 2025  
**المرحلة:** Sprint 0.2 - إعداد البيئة والأدوات  
**الإصدار:** 1.0

---

## 📋 نظرة عامة

يستخدم مشروع Desktop Management System استراتيجية **Git Flow** المعدلة لضمان تطوير منظم وآمن. هذا الدليل يوضح جميع القواعد والإجراءات المطلوبة للعمل مع النظام.

---

## 🌳 هيكل الفروع

### الفروع الرئيسية:

#### 🏠 `main`
- **الغرض:** الإصدارات المستقرة والجاهزة للإنتاج
- **الحماية:** محمي - لا يمكن الدفع المباشر
- **التحديث:** فقط من خلال Pull Requests من `develop` أو `hotfix/*`
- **العلامات:** جميع الإصدارات تحمل tags (v1.0.0, v1.1.0, إلخ)

#### 🔧 `develop`
- **الغرض:** التطوير النشط والتكامل
- **الحماية:** محمي جزئياً - PR مطلوب
- **التحديث:** من `feature/*` و `release/*` branches
- **الاختبارات:** يجب أن تمر جميع الاختبارات قبل الدمج

---

## 🔀 فروع العمل

### 🚀 `feature/*`
**التسمية:** `feature/[sprint-number]-[feature-name]`

**أمثلة:**
- `feature/1.1-project-management`
- `feature/1.2-financial-dashboard`
- `feature/2.1-advanced-reporting`

**دورة الحياة:**
1. إنشاء من `develop`
2. التطوير والاختبار
3. Pull Request إلى `develop`
4. مراجعة الكود والموافقة
5. الدمج والحذف

### 🎯 `release/*`
**التسمية:** `release/v[major].[minor].[patch]`

**أمثلة:**
- `release/v1.0.0`
- `release/v1.1.0`
- `release/v2.0.0`

**الاستخدام:**
- تحضير الإصدارات الجديدة
- إصلاح الأخطاء الطفيفة
- تحديث أرقام الإصدارات
- اختبارات نهائية

### 🚨 `hotfix/*`
**التسمية:** `hotfix/v[major].[minor].[patch]-[issue-description]`

**أمثلة:**
- `hotfix/v1.0.1-security-patch`
- `hotfix/v1.0.2-critical-bug-fix`

**الاستخدام:**
- إصلاح الأخطاء الحرجة في الإنتاج
- إنشاء من `main`
- دمج في `main` و `develop`

---

## 📝 قواعد التسمية

### الفروع:
- **استخدم الأحرف الصغيرة والشرطات**
- **ابدأ بنوع الفرع** (`feature/`, `release/`, `hotfix/`)
- **استخدم أسماء وصفية وواضحة**
- **تجنب المسافات والأحرف الخاصة**

### الـ Commits:
```
نوع(النطاق): وصف مختصر

وصف مفصل (اختياري)

- تفاصيل إضافية
- مراجع للـ issues

Closes #123
```

**أنواع الـ Commits:**
- `feat:` - ميزة جديدة
- `fix:` - إصلاح خطأ
- `docs:` - تحديث الوثائق
- `style:` - تنسيق الكود
- `refactor:` - إعادة هيكلة
- `test:` - إضافة اختبارات
- `chore:` - مهام صيانة

---

## 🔄 سير العمل

### 1. بدء ميزة جديدة:
```bash
# التأكد من أحدث إصدار من develop
git checkout develop
git pull origin develop

# إنشاء فرع جديد
git checkout -b feature/1.1-project-management

# العمل والتطوير
git add .
git commit -m "feat(projects): إضافة واجهة إدارة المشاريع الأساسية"

# دفع الفرع
git push origin feature/1.1-project-management
```

### 2. إنشاء Pull Request:
1. انتقل إلى GitHub
2. اختر "New Pull Request"
3. من: `feature/1.1-project-management`
4. إلى: `develop`
5. املأ القالب المطلوب
6. اطلب المراجعة

### 3. مراجعة ودمج:
- **مراجعة الكود** من قبل عضو آخر في الفريق
- **تشغيل الاختبارات** التلقائية
- **فحص الجودة** والمعايير
- **الموافقة والدمج**

### 4. تحضير إصدار:
```bash
# إنشاء فرع الإصدار
git checkout develop
git checkout -b release/v1.1.0

# تحديث أرقام الإصدارات
# اختبارات نهائية
# إصلاح أخطاء طفيفة

# دمج في main
git checkout main
git merge release/v1.1.0
git tag v1.1.0

# دمج في develop
git checkout develop
git merge release/v1.1.0

# حذف فرع الإصدار
git branch -d release/v1.1.0
```

---

## 🛡️ قواعد الحماية

### فرع `main`:
- ✅ مطلوب Pull Request
- ✅ مطلوب مراجعة من عضوين
- ✅ يجب نجاح جميع الاختبارات
- ✅ يجب نجاح فحص الجودة
- ❌ لا يمكن الدفع المباشر
- ❌ لا يمكن حذف التاريخ

### فرع `develop`:
- ✅ مطلوب Pull Request
- ✅ مطلوب مراجعة من عضو واحد
- ✅ يجب نجاح الاختبارات الأساسية
- ⚠️ يمكن للمديرين الدفع المباشر (للطوارئ)

---

## 📊 مؤشرات الأداء

### مقاييس Git:
- **معدل الـ Commits:** 5-10 commits يومياً
- **حجم الـ PR:** أقل من 500 سطر
- **وقت المراجعة:** أقل من 24 ساعة
- **معدل الدمج:** 95% من الـ PRs تُدمج بنجاح

### جودة الكود:
- **تغطية الاختبارات:** أكثر من 80%
- **معايير الكود:** 100% compliance
- **الثغرات الأمنية:** 0 high/critical

---

## 🚨 إجراءات الطوارئ

### في حالة خطأ حرج في الإنتاج:
1. **إنشاء hotfix branch** من `main`
2. **إصلاح سريع** مع اختبار محدود
3. **PR عاجل** مع مراجعة سريعة
4. **دمج في `main`** ونشر فوري
5. **دمج في `develop`** لتجنب التضارب

### في حالة تضارب كبير:
1. **توقف عن الدمج**
2. **تحليل التضارب** مع الفريق
3. **حل يدوي** أو إعادة كتابة
4. **اختبار شامل** قبل المتابعة

---

## 📚 مراجع إضافية

- [Git Flow الأصلي](https://nvie.com/posts/a-successful-git-branching-model/)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)

---

**📝 ملاحظة:** هذا الدليل قابل للتحديث حسب احتياجات المشروع وتطور الفريق.
