# 🎨 Color Consolidation Phase 1

## الهدف

توحيد جميع دوال الألوان (الحالة، الأولوية، الصحة، التقدم) في مصدر واحد بدل التشتت بين helpers / centralData / الدوال المحلية داخل المكوّنات.

## قبل

- helpers.ts: دوال متكررة.
- centralData.ts: نسخة مختلفة لـ getHealthColor.
- مكوّنات: منطق مضمَّن (Dashboard, TenderDetails, Budgets...).

## بعد

مصدر واحد: `src/utils/statusColors.ts`

يوفر:

- خرائط STATUS / PRIORITY / HEALTH
- دالة `getProgressColor`
- دالة عامة `getSemanticColor`
- دالة مساعدة للاختبارات `__debugSemanticMaps`
- واجهة مستقبلية قابلة للتوسعة (تغليف لاحق بدلاً من سلاسل Tailwind)

## فوائد

| المشكلة السابقة | الحل | الفائدة |
|-----------------|------|---------|
| تكرار وتعريفات مختلفة | ملف موحد | صيانة أسهل |
| صعوبة تغيير التصميم | نقطة مركزية | دعم theme مستقبلي |
| اختبارات غير موجودة | اختبارات جديدة | حماية من الانحراف |

## التوافق

تم إبقاء دوال @deprecated في `helpers.ts` مؤقتاً لتفادي كسر أي استيراد قديم.

## المخاطر المحتملة

- مطور يضيف منطق ألوان جديد خارج المصدر الموحد → علاج: lint أو مراجعة.
- اختلاف ألوان متوقعة في UI قديمة → تمت المحافظة على نفس القيم الأساسية.

## الاختبارات المضافة

`tests/utils/statusColors.test.ts` تغطي:

- جميع الحالات الأساسية
- الأولويات
- الصحة + fallback
- التقدم بنقاط فاصلة
- الخرائط غير فارغة

## الخطوات التالية (Phase 2)

1. إزالة الأغلفة @deprecated بعد التأكد من صفر استخدام خارجي.
2. إضافة Aliases للصحة (good→green, warning→yellow, critical→red).
3. إرجاع كائنات نمطية بدلاً من سلاسل (fg/bg) عندما نُدخل طبقة Theme.
4. إضافة Rule داخل ESLint (اختياري) لمنع تعريف ألوان جديدة يدوياً.

## مؤشرات النجاح

- جميع الاختبارات 100% تمر.
- انخفاض grep لعدد تكرارات `bg-green-500` على المدى المتوسط.
- عدم ظهور أي تعريف جديد خارج statusColors.

## سجل التنفيذ المختصر

1. إنشاء statusColors.ts.
2. تحديث المكوّنات الرئيسية لاستيراده.
3. إزالة getHealthColor المحلي من centralData وإعادة التصدير.
4. وضع @deprecated في helpers.
5. إضافة اختبارات.
6. توثيق هذه المرحلة.

## مسؤولية المتابعة

- عند اكتمال Phase 2: حذف الدوال المتقادمة نهائياً.

تاريخ: 20 سبتمبر 2025
