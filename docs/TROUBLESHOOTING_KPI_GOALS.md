# استكشاف أخطاء الأهداف في بطاقات مؤشرات الأداء

## المشكلة المبلغ عنها

**الوصف:** تم إدخال هدفين في صفحة "إدارة التطوير"، لكنهما لا يظهران في بطاقات الأهداف السنوية.

## خطوات التشخيص

### 1. التحقق من تخزين الأهداف

افتح Developer Console في المتصفح وابحث عن السجلات التالية:

```
📖 [DevelopmentGoalsRepository] Loading from key: development_goals
📖 [DevelopmentGoalsRepository] Raw stored data: [...]
📖 [DevelopmentGoalsRepository] Returning goals count: X
```

**إذا كان count = 0:**

- الأهداف لم يتم حفظها بشكل صحيح
- تحقق من وجود أخطاء عند الحفظ في صفحة "إدارة التطوير"

### 2. التحقق من تحميل Store

ابحث عن السجلات:

```
🔄 [DevelopmentGoalsStore] Starting hydration...
✅ [DevelopmentGoalsStore] Loaded from repository: [...]
✅ [DevelopmentGoalsStore] Goals count: X
```

**إذا لم تظهر هذه السجلات:**

- Store لم يتم تهيئته (hydrate)
- تحقق من أن `useDevelopment` hook يتم استدعاؤه

### 3. التحقق من قراءة الأهداف في المكونات

ابحث عن السجلات:

```
📊 [AnnualKPICards] Goals loaded: [...]
📊 [AnnualKPICards] Goals count: X
🎯 [useKPIs] Goals loaded: [...]
```

**إذا كان count = 0 هنا:**

- Store لم يحمل البيانات بعد
- المكون يعرض قبل اكتمال hydration

### 4. التحقق من مطابقة الفئة والسنة

ابحث عن السجلات:

```
🎯 [getYearlyTarget] Category: projects, Year: 2025
🎯 [getYearlyTarget] Goal found: {...}
🎯 [getYearlyTarget] Key: targetValue2025, Value: X
```

**إذا كان Goal found: undefined:**

- الفئة (category) غير مطابقة
- تأكد من أن الهدف المُدخل له نفس الفئة المطلوبة

**إذا كان Value: undefined:**

- الحقل `targetValue2025` غير موجود أو قيمته غير صحيحة
- تأكد من أن السنة المحددة صحيحة

## الفئات المطلوبة

يجب أن تطابق فئة الهدف (category) إحدى القيم التالية:

- `tenders` - للمنافسات
- `projects` - للمشاريع
- `revenue` - للإيرادات
- `profit` - للأرباح
- `performance` - لأداء المشاريع

## صيغة الهدف الصحيحة

```json
{
  "id": "goal-xyz",
  "title": "هدف عدد المشاريع",
  "category": "projects",
  "type": "yearly",
  "unit": "number",
  "currentValue": 5,
  "targetValue2025": 25,
  "targetValue2026": 30,
  "targetValue2027": 35,
  "description": "الوصول إلى 25 مشروع بحلول نهاية 2025"
}
```

### الحقول المطلوبة:

- `category`: يجب أن يكون من القائمة أعلاه
- `type`: يجب أن يكون `"yearly"` (وليس `"monthly"`)
- `targetValue2025`: رقم صحيح (ليس null أو undefined)

## الحلول الشائعة

### الحل 1: التحقق من الفئة

تأكد من أن الأهداف المُدخلة لها فئة صحيحة:

```typescript
// ✅ صحيح
{
  category: "projects",
  type: "yearly",
  targetValue2025: 25
}

// ❌ خطأ
{
  category: "project", // بدون s
  type: "yearly",
  targetValue2025: 25
}
```

### الحل 2: التحقق من النوع

تأكد من أن النوع هو `"yearly"`:

```typescript
// ✅ صحيح
{
  type: 'yearly'
}

// ❌ خطأ
{
  type: 'monthly'
}
```

### الحل 3: التحقق من السنة

تأكد من أن الحقل المناسب للسنة الحالية موجود:

```typescript
// السنة الحالية: 2025
{
  targetValue2025: 25, // ✅ صحيح
  targetValue2026: 30,
  targetValue2027: 35
}
```

### الحل 4: إعادة إدخال الأهداف

إذا كانت الأهداف موجودة لكن بصيغة خاطئة:

1. احذف الأهداف الموجودة من صفحة "إدارة التطوير"
2. أعد إدخالها من جديد
3. تأكد من اختيار الفئة والنوع الصحيحين
4. احفظ وتحقق من الـ console

## صفحة التشخيص

تم إنشاء صفحة خاصة للتشخيص:

```
src/presentation/pages/Debug/DebugGoals.tsx
```

يمكن استخدامها لعرض:

- الأهداف من Hook
- الأهداف من LocalStorage مباشرة
- تفاصيل كل هدف

## الفحص اليدوي

يمكنك فحص LocalStorage يدوياً:

1. افتح Developer Console
2. انتقل إلى Application > Local Storage
3. ابحث عن المفتاح `development_goals`
4. تحقق من البيانات المخزنة

## أمثلة على السجلات الصحيحة

### عند الحفظ:

```
📖 [DevelopmentGoalsRepository] Loading from key: development_goals
📖 [DevelopmentGoalsRepository] Raw stored data: [{...}, {...}]
📖 [DevelopmentGoalsRepository] Goals count: 2
✅ [DevelopmentGoalsStore] Loaded from repository: [{...}, {...}]
✅ [DevelopmentGoalsStore] Goals count: 2
```

### عند القراءة:

```
📊 [AnnualKPICards] Goals loaded: [{...}, {...}]
📊 [AnnualKPICards] Goals count: 2
🎯 [getYearlyTarget] Category: projects, Year: 2025
🎯 [getYearlyTarget] Goal found: {...}
🎯 [getYearlyTarget] Value: 25
```

## جدول استكشاف الأخطاء

| الأعراض                                     | السبب المحتمل           | الحل                 |
| ------------------------------------------- | ----------------------- | -------------------- |
| Goals count: 0 في Repository                | لم يتم الحفظ            | أعد إدخال الأهداف    |
| Goals count: 2 في Repository لكن 0 في Store | مشكلة Hydration         | أعد تحميل الصفحة     |
| Goal found: undefined                       | فئة خاطئة               | تحقق من category     |
| Value: undefined                            | حقل السنة مفقود         | أعد إدخال الهدف      |
| البطاقة تعرض القيمة الافتراضية (fallback)   | لم يتم العثور على الهدف | تحقق من الفئة والنوع |

## معلومات إضافية

### مفتاح التخزين

```typescript
STORAGE_KEYS.DEVELOPMENT_GOALS = 'development_goals'
```

### الفئات المدعومة

```typescript
type Category = 'tenders' | 'projects' | 'revenue' | 'profit' | 'performance'
```

### أنواع الأهداف

```typescript
type GoalType = 'monthly' | 'yearly'
```

**ملاحظة:** حالياً، البطاقات تعرض فقط الأهداف السنوية (`yearly`).
