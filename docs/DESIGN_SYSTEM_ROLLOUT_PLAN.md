# خطة تنفيذ نظام التصميم الموحد

> **الإصدار:** 1.0.0  
> **تاريخ الإعداد:** 9 أكتوبر 2025  
> **إعداد:** فريق تجربة المستخدم والتطوير الواجهات

هذه الخطة تحدد مراحل وخطوات تطبيق نظام التصميم الموحد على جميع صفحات ومكونات النظام، مع مراعاة أفضل الممارسات في الحوكمة، التطوير التدريجي، وضمان الجودة.

---

## المرحلة 0 – التحضير والحوكمة

| الهدف | بناء إطار الحوكمة وضمان جاهزية الفريق قبل بدء التنفيذ |
|-------|-----------------------------------------------------------|

**حالة التنفيذ:** ✅ تم إعداد حزمة الحوكمة والجاهزية الأولية في `docs/design-system/` (GOVERNANCE.md, ACCEPTANCE_CRITERIA.md, DECISIONS.md, MEASUREMENT_SETUP.md).

1. **تشكيل فريق التنفيذ المتعدد التخصصات**
   - تعيين قائد للتجربة البصرية وقائد تقني.
   - تحديد ممثلين عن الفريق الأمامي، الخلفي، ومديري المنتجات.
2. **تعريف معايير القبول والنجاح**
   - صياغة تعريف «الجاهزية» (Definition of Ready) لكل مكون/صفحة قبل إعادة بنائها.
   - وضع معايير تضمن توافق الـ WCAG والتجاوب وRTL.
3. **تحديث دليل الحوكمة**
   - إنشاء قناة اتصال مخصصة (Slack / Teams) لتوثيق القرارات.
   - توثيق سياسات المراجعة (PR checklist، توافق التصميم).
4. **إعداد بيئة القياس**
   - تفعيل Storybook أو Ladle لتوثيق المكونات المعتمدة.
   - تأكيد وجود أداة تحليل التغطية البصرية (Visual Regression) إن وُجدت.

📦 **المخرجات:** وثيقة حوكمة محدثة، لوحة متابعة (Kanban) للمهام، Storybook مُهيأ.

---

## المرحلة 1 – تدقيق شامل ومواءمة الأرضية المشتركة

| الهدف | حصر الوضع الحالي وتحديد عناصر النظام المرجعي التي يجب تعميمها |
|-------|-----------------------------------------------------------------|

**حالة التنفيذ:** ✅ اكتملت المرحلة 1. تم إصدار `PHASE1_AUDIT_REPORT.md`، وتوثيق الانحرافات اللونية، وإنشاء `COMPONENT_INVENTORY.md` كمرجع للحالة الحالية.

1. **مراجعة التصميم المرجعي**
   - التأكد من اكتمال `docs/DESIGN_SYSTEM.md` وتوافقه مع `tokens.config.ts` و `globals.css`.
2. **إعداد قائمة بالانحرافات**
   - استخراج جميع الألوان والظلال خارج نطاق الـ Tokens باستخدام أداة بحث تلقائية.
   - رصد المكوّنات التي لا تستخدم `PageLayout`, `WidgetContainer`, أو مكونات `ui` القياسية.
3. **تحديد أولويات المكونات الحرجة**
   - تصنيف الانحرافات إلى: أساسية (Layout, Header, Sidebar)، متوسطة (بطاقات، Badges)، منخفضة (صفحات ثانوية).
4. **إنشاء سجل المكونات (Component Inventory)**
   - جدول يوضح كل مكون، حالته (مطابق/غير مطابق)، وخطة المعالجة.

📦 **المخرجات:** تقرير الانحرافات، سجل المكونات، خارطة أولويات التنفيذ.

---

## المرحلة 2 – تحديث البنية التحتية للتصميم

| الهدف | تأمين الأساس الفني لضمان سهولة التبني وإعادة الاستخدام |
|-------|-----------------------------------------------------------|

1. **مواءمة إدارة السمات**
   - إزالة ازدواجية إدارة الوضع الداكن من `AppShell` والاعتماد الكامل على `ThemeProvider`.
   - إضافة دعم صريح لـ High Contrast في مكونات التحكم.
2. **تحديث الأدوات والتكوينات**
   - إضافة قواعد ESLint/Tailwind تمنع استخدام الألوان الثابتة خارج الـ Tokens.
   - توفير وظائف مساعدة لقراءة الألوان داخل TypeScript حيث يلزم.
3. **بناء مكتبة مكونات أساسية**
   - استخراج المكونات المشتركة (Header, Sidebar, PageHeader) إلى مكتبة `@/components/ui/layout`.
   - إنشاء مكونات هوية (StatusBadge, InlineAlert, ActionBar) مبنية على التصميم الموحد.
4. **إعداد أمثلة Storybook**
   - توثيق حالات المكونات الأساسية الثلاث (Light/Dark/High Contrast) لضمان الاتساق البصري.

📦 **المخرجات:** مكتبة مكونات أساسية محدثة، قواعد lint جديدة، قصص Storybook.

---

## المرحلة 3 – إعادة بناء المكونات المشتركة

| الهدف | ضمان أن المكونات الهيكلية الرئيسية متوافقة بالكامل قبل الانتقال للصفحات |
|-------|------------------------------------------------------------------------|

1. **تحديث `PageLayout`**
   - استبدال الألوان الثابتة بـ `bg-background`, `bg-card`, `text-muted-foreground`.
   - ضمان ظهور زر الرجوع تلقائيًا عند توفر `onBack`.
   - إضافة دعم `prefers-reduced-motion` للحركات.
2. **ضبط `Header` و`Sidebar`**
   - توحيد الألوان باستخدام `card`, `muted`, `accent`.
   - إعادة ترتيب أزرار الإجراءات لتتبع النمط الرسمي (زر الإنشاء على اليسار، البقية متجانسة).
3. **تحديث مكونات البطاقات/الرسائل**
   - تطبيق `StatusBadge`, `InlineAlert` على `TenderCard`, `ProjectCard`, وغيرها.
   - تعميم استخدام `WidgetContainer` أو نمط مشابه للبطاقات.
4. **اختبار الانحدار البصري**
   - التقاط لقطات قبل/بعد لضمان عدم تغير الوظائف أو الـ UX سلبًا.

📦 **المخرجات:** مكونات مشتركة متوافقة، نتائج اختبارات بصرية مثبتة، توثيق تغييرات التبعية.

---

## المرحلة 4 – ترحيل الصفحات حسب الأولوية

| الهدف | تحديث الصفحات تدريجيًا لضمان الاستقرار والسرعة |
|-------|-------------------------------------------------|

1. **موجة 1 – الصفحات عالية التكرار**
   - لوحة التحكم (`AdvancedDashboard`)، المشاريع (`Projects`)، المنافسات (`Tenders`).
   - ضمان استخدام المكونات المحدثة وتطبيق الألوان الدلالية.
2. **موجة 2 – نماذج الإدخال المعقدة**
   - `NewProjectForm`, `NewTenderForm`, `TenderPricingWizard`.
   - ضبط الحقول، الأزرار، الرسائل، والحالة الفارغة وفق النظام.
3. **موجة 3 – الصفحات الثانوية والتقارير**
   - التقارير المالية، الإعدادات، صفحات إدارة الحسابات.
   - مراجعة تخطيطات الجداول والبطاقات.
4. **لكل موجة**
   - تنفيذ تحليل UX سريع (Heuristic Review) والتأكد من دعم RTL والتباين.
   - اختبار وظيفي (Smoke Tests) + فحص لقطات VisReg.

📦 **المخرجات:** صفحات محدثة وفق الأولويات، تقارير مراجعة UX، سجل نتائج الاختبارات.

---

## المرحلة 5 – ضمان الجودة والتوثيق

| الهدف | تثبيت النتائج وضمان عدم التراجع |
|-------|-----------------------------------|

1. **إجراء اختبارات شاملة**
   - Vitest للوحدات، Playwright للواجهات، ولمن يلزم Electron smoke tests.
2. **تحديث الوثائق**
   - توسيع `DESIGN_SYSTEM.md` بجدول «قبل/بعد» وأمثلة تعليمية.
   - إعداد دليل «كيفية الالتزام بالتصميم الموحد» للمطورين.
3. **نشر دليل مراجعة PR**
   - قائمة تحقق تشمل: استخدام tokens، التباين، التوافق مع السمات، دعم RTL.
4. **بناء لوحة مراقبة**
   - Dashboard في Storybook أو Documentation site يعرض حالة التوافق لكل صفحة.

📦 **المخرجات:** تغطية اختبارات محدثة، وثائق موسعة، دليل مراجعة PR، لوحة مراقبة التوافق.

---

## المرحلة 6 – التحسين المستمر والمراقبة

| الهدف | منع التراجع وضمان تبني الفريق للهوية الجديدة |
|-------|------------------------------------------------|

1. **إطلاق نسخة داخلية (Beta)**
   - جمع ملاحظات المستخدمين النهائيين وفرق الدعم.
2. **مراقبة مؤشرات الأداء**
   - تتبع مؤشرات مثل سرعة التطوير، وقت مراجعة PR، شكاوى UX.
3. **جلسات تدريب شهرية**
   - ورش عمل قصيرة لعرض حالات الاستخدام الجيدة وتبادل الخبرات.
4. **جدولة مراجعات دورية**
   - مراجعة ربع سنوية للنظام لضمان تحديث الـ Tokens ومواكبة المتطلبات.

📦 **المخرجات:** تقارير تغذية راجعة، مؤشرات أداء مُحدثة، خطة تدريب، جدول مراجعات.

---

## الجدول الزمني المقترح

| المرحلة | المدة التقديرية | نقاط التعليق |
|---------|-----------------|----------------|
| 0 | أسبوع | تجهيز الفريق والحوكمة |
| 1 | أسبوعان | يكتمل بعد تسليم تقرير الانحرافات |
| 2 | 2-3 أسابيع | يعتمد على حجم refactoring للمكونات الأساسية |
| 3 | 2 أسابيع | يمكن تنفيذها متوازية جزئيًا |
| 4 | 4-6 أسابيع | تقسيم الموجات وتداخلها بناءً على الأولوية |
| 5 | أسبوع | اختبار نهائي وتوثيق |
| 6 | مستمر | مراقبة وتحسين |

> **ملاحظة:** يمكن تكييف الجدول بناءً على توافر الفريق وحجم التعديلات المطلوبة في كل صفحة.

---

## عوامل النجاح الحرجة

- الالتزام الصارم باستخدام الـ Tokens والمكونات الموحدة في جميع التعديلات الجديدة.
- وجود حوكمة نشطة ومراجعة مشتركة بين فرق التصميم والتطوير.
- تطبيق اختبارات بصرية ووظيفية بعد كل دفعة تحديث.
- توثيق مستمر وتدريب للفريق لمنع العودة إلى الأنماط القديمة.

---

## المتابعة والقياس

- **لوحة قياس (Dashboard):** نسبة الصفحات الملتزمة، عدد الانحرافات المفتوحة، وقت معالجة الانحراف.
- **مؤشرات تجربة المستخدم:** معدل رضا المستخدمين الداخليين، انخفاض الشكاوى عن التباين أو التعارض البصري.
- **مؤشرات الفريق:** سرعة تطوير المكونات، زمن مراجعة PR، نسبة إعادة العمل.

---

باتباع هذه الخطة المتسلسلة، يمكن توحيد الهوية البصرية للنظام تدريجيًا دون التأثير على استقرار المنصة، مع ضمان تبني الفريق للممارسات الموحدة وتعظيم الفائدة من نظام التصميم المركزي.
