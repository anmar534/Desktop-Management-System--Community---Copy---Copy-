# 📐 دليل تخطيط لوحة التحكم المتقدمة

## 🎯 الهدف
تحسين التخطيط المرئي للوحة التحكم لضمان عرض جميع البطاقات (Widgets) في صفوف أفقية دون تكدس عمودي غير مرغوب.

---

## 📊 نظام الشبكة (Grid System)

### Breakpoints وعدد الأعمدة

| Breakpoint | Min Width | عدد الأعمدة | الوصف |
|------------|-----------|-------------|--------|
| `lg` | 1200px | 12 | الشاشات الكبيرة (Desktop) |
| `md` | 996px | 10 | الشاشات المتوسطة (Tablet Landscape) |
| `sm` | 768px | 8 | التابلت العمودي |
| `xs` | 480px | 4 | الموبايل الأفقي |
| `xxs` | 0px | 2 | الموبايل العمودي |

---

## 🧩 أنواع البطاقات وأبعادها

### 1️⃣ البطاقات الصغيرة (Small Cards)
**مثال:** `cash`, `runway`, `projects`, `tenders`

| Breakpoint | العرض (Columns) | الملاحظات |
|------------|----------------|-----------|
| `lg` | 3 | ربع الشاشة (~25%) |
| `md` | 2 | خُمس الشاشة (~20%) |
| `sm` | 4 | نصف الشاشة (~50%) |
| `xs` | **2** | **نصف الشاشة (بطاقتان جنباً لجنب)** ✅ |
| `xxs` | 2 | كامل العرض (100%) |

---

### 2️⃣ البطاقات الكبيرة (Large Cards)
**مثال:** `cashflow`, `expense`

| Breakpoint | العرض (Columns) | الملاحظات |
|------------|----------------|-----------|
| `lg` | 6 | نصف الشاشة (~50%) |
| `md` | 5 | نصف الشاشة (~50%) |
| `sm` | 8 | كامل العرض (100%) |
| `xs` | **4** | **كامل العرض** |
| `xxs` | 2 | كامل العرض |

---

### 3️⃣ البطاقات المتوسطة (Medium Cards)
**مثال:** `deadlines`, `insights`, `team`, `financial`, `calendar`, `documents`, `micro`, `projectHealth`, `invoiceAging`

| Breakpoint | العرض (Columns) | الملاحظات |
|------------|----------------|-----------|
| `lg` | 4 | ثلث الشاشة (~33%) |
| `md` | 5 | نصف الشاشة (~50%) |
| `sm` | 4 | نصف الشاشة (~50%) |
| `xs` | **2** | **نصف الشاشة (بطاقتان جنباً لجنب)** ✅ |
| `xxs` | 2 | كامل العرض |

---

## ✅ التحسينات المطبقة

### 1. تغيير `compactType`
```tsx
// قبل:
compactType={null}

// بعد:
compactType="vertical"  // يضغط البطاقات عمودياً بشكل محكم
```

### 2. تقليل عرض البطاقات المتوسطة في `xs`
```typescript
// قبل: كانت تأخذ كامل العرض (4 أعمدة)
widths: { md: 5, sm: 4, xs: 4, xxs: 2 }

// بعد: أصبحت تأخذ نصف العرض (عمودين)
widths: { md: 5, sm: 4, xs: 2, xxs: 2 }
```

---

## 📐 النتيجة المتوقعة

### على شاشات `xs` (480px - 767px):
- **البطاقات الصغيرة:** بطاقتان في كل صف (2+2=4) ✅
- **البطاقات المتوسطة:** بطاقتان في كل صف (2+2=4) ✅
- **البطاقات الكبيرة:** بطاقة واحدة في كل صف (4 أعمدة = 100% العرض) ✅

### على شاشات `sm` (768px - 995px):
- **البطاقات الصغيرة:** بطاقتان في كل صف (4+4=8)
- **البطاقات المتوسطة:** بطاقتان في كل صف (4+4=8)
- **البطاقات الكبيرة:** بطاقة واحدة في كل صف (8 أعمدة = 100%)

### على شاشات `md` (996px - 1199px):
- **البطاقات الصغيرة:** 5 بطاقات في كل صف (2×5=10)
- **البطاقات المتوسطة:** بطاقتان في كل صف (5+5=10)
- **البطاقات الكبيرة:** بطاقتان في كل صف (5+5=10)

### على شاشات `lg` (1200px+):
- **البطاقات الصغيرة:** 4 بطاقات في كل صف (3×4=12)
- **البطاقات المتوسطة:** 3 بطاقات في كل صف (4×3=12)
- **البطاقات الكبيرة:** بطاقتان في كل صف (6+6=12)

---

## 🔧 الملفات المعدّلة

1. **`src/features/dashboard/AdvancedDashboard.tsx`**
   - تغيير `compactType` من `null` إلى `"vertical"`
   - إضافة `w-full` للـ `<section>` الرئيسي
   - إضافة `w-full` لـ `ResponsiveGridLayout`

2. **`src/features/dashboard/presets/index.ts`**
   - تعديل `WIDGET_DIMENSIONS` لجميع البطاقات المتوسطة
   - تقليل `xs` من 4 إلى 2 أعمدة

3. **`src/App.tsx`**
   - إضافة `w-full p-4` للـ `<main>` لضمان استخدام كامل العرض المتاح
   - إصلاح مشكلة العمود الضيق

4. **`src/types/react-grid-layout.d.ts`**
   - إضافة `Layouts` type export

---

## 🧪 الاختبار

جميع الاختبارات (186 اختباراً) نجحت بعد التعديلات:
```bash
npm run test
# Test Files  62 passed (62)
# Tests  186 passed (186)
```

---

## 📝 ملاحظات إضافية

- **RTL Support:** التخطيط يدعم العربية بشكل كامل
- **responsive:** يتكيف تلقائياً مع جميع أحجام الشاشات
- **Draggable:** يمكن سحب وإسقاط البطاقات في وضع `custom`
- **Resizable:** يمكن تغيير حجم البطاقات في وضع `custom`

---

## 🎨 التخصيص المستقبلي

لإضافة بطاقة جديدة:

1. أضف تعريفها في `WIDGET_DIMENSIONS`
2. حدد العرض لكل breakpoint
3. أضفها إلى ترتيب preset مناسب (`EXECUTIVE_ORDER`, `FINANCIAL_ORDER`, إلخ)

```typescript
newWidget: {
  id: 'newWidget',
  defaultWidth: 4,     // على lg
  defaultHeight: 3,
  widths: { 
    md: 5,   // على md
    sm: 4,   // على sm
    xs: 2,   // على xs (نصف العرض)
    xxs: 2   // على xxs (كامل العرض)
  },
}
```

---

**آخر تحديث:** 2025-01-09
