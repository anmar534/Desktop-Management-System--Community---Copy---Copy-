# 📊 تقرير تحليل الملفات - مكونات لوحة التحكم

## 1️⃣ DashboardKPICards.tsx

**📍 المسار**: `src/presentation/pages/Dashboard/components/DashboardKPICards.tsx`

### 🎯 الغرض الرئيسي

مكون **عرض** مجموعة بطاقات مؤشرات الأداء الرئيسية (KPI) في لوحة التحكم الرئيسية.

### 🔧 الوظائف

- **عرض البطاقات**: يستقبل قائمة بيانات KPI ويعرضها في شبكة Grid
- **إدارة الحالات**:
  - ⏳ Loading: يعرض 5 بطاقات skeleton متحركة
  - 🚫 No Goals: رسالة + زر للانتقال لإدارة التطوير
  - 📋 No Selection: رسالة + زر لتخصيص المؤشرات
  - ✅ Normal: عرض البطاقات المختارة

### 📦 المدخلات (Props)

```typescript
{
  kpis: KPICardData[]           // البيانات المحسوبة من useKPIs()
  isLoading: boolean            // حالة التحميل
  maxCards: number              // الحد الأقصى (5)
  hasGoals: boolean             // هل يوجد أهداف؟
  onSectionChange: Function     // التنقل للصفحات
  onAddGoals: Function          // إضافة أهداف
  onCustomize: Function         // فتح نافذة التخصيص
}
```

### 🎨 التخطيط

- Grid: `grid-cols-1 md:grid-cols-2 xl:grid-cols-5`
- يستخدم: `UnifiedKPICard` لكل بطاقة فردية

### 🔗 الاستخدام

```tsx
<DashboardKPICards
  kpis={visibleKpis}
  isLoading={kpiLoading}
  maxCards={5}
  hasGoals={allKpis.length > 0}
  onSectionChange={onSectionChange}
  onAddGoals={() => onSectionChange('development')}
  onCustomize={() => setPreferencesOpen(true)}
/>
```

### ✨ المميزات

- ✅ واجهة نظيفة لجميع الحالات
- ✅ Loading state احترافي
- ✅ رسائل إرشادية واضحة
- ✅ أزرار إجراءات سريعة

---

## 2️⃣ TenderStatusCards.tsx

**📍 المسار**: `src/presentation/pages/Tenders/components/TenderStatusCards.tsx`

### 🎯 الغرض الرئيسي

عرض **بطاقتين رئيسيتين** عن حالة المنافسات:

1. **المنافسات العاجلة** (Urgent Tenders)
2. **تحليل أداء المنافسات** (Performance Analysis)

### 🔧 الوظائف

#### 📌 البطاقة الأولى: المنافسات العاجلة

- عرض المنافسات التي تنتهي خلال 7 أيام
- عرض أبرز 4 منافسات عاجلة
- إحصائيات:
  - عدد المنافسات العاجلة
  - المنافسات المنتهية
  - المنافسات قيد الإجراء
- تصنيف حسب الأولوية:
  - 🔴 Overdue: ≤1 يوم
  - 🟡 Due Soon: ≤3 أيام
  - 🔵 On Track: >3 أيام
- أزرار سريعة:
  - استعراض المنافسات
  - إضافة منافسة جديدة

#### 📌 البطاقة الثانية: تحليل الأداء

- مقارنة الأداء الشهري (الحالي vs الماضي):
  - معدل الفوز (Win Rate)
  - عدد المنافسات المقدمة
  - قيمة المنافسات
- مؤشرات:
  - معدل الفوز: نسبة مئوية
  - معدل القيمة: مبلغ بالملايين
  - معدل العطاءات المقدمة
- أزرار سريعة:
  - تقرير مفصل
  - عرض سريع

### 📊 مصادر البيانات

```typescript
const { tenders } = useTenderListStore() // قائمة المنافسات
const { stats } = useTenders() // الإحصائيات المحسوبة
```

### 🎨 التخطيط

- **الحالي**: عرض البطاقتين جنباً إلى جنب (في Fragment)
- **في لوحة التحكم**: `grid-cols-3` - تأخذان مساحتين من 3

### 📦 المدخلات

```typescript
{
  onSectionChange: Function // للتنقل بين الصفحات
}
```

### ✨ المميزات

- ✅ تحديث فوري للبيانات
- ✅ تنبيهات ذكية للمنافسات العاجلة
- ✅ مقارنات شهرية تلقائية
- ✅ أيقونات ملونة حسب الحالة
- ✅ رسوم متحركة سلسة (Framer Motion)

---

## 3️⃣ AnnualKPICards.tsx

**📍 المسار**: `src/presentation/pages/Dashboard/components/AnnualKPICards.tsx`

### 🎯 الغرض الرئيسي

عرض **4 بطاقات ثابتة** لمؤشرات الأداء السنوية الأساسية.

### 📊 البطاقات الأربعة

#### 1. نسبة فوز المنافسات 🏆

- **القيمة الحالية**: من `tenderStats.winRate`
- **الهدف السنوي**: من إدارة التطوير (افتراضي: 80%)
- **اللون**: أصفر (warning)

#### 2. عدد المشاريع 🏢

- **القيمة الحالية**: إجمالي المشاريع
- **الهدف السنوي**: من إدارة التطوير (افتراضي: 25)
- **اللون**: أزرق (primary)
- **التفاصيل**: "X نشط، Y مكتمل"

#### 3. الإيرادات السنوية 💵

- **القيمة الحالية**: قيمة المنافسات الفائزة بالملايين
- **الهدف السنوي**: من إدارة التطوير (افتراضي: 60 مليون)
- **اللون**: أخضر (success)
- **التفاصيل**: "نمو X% عن العام الماضي"

#### 4. متوسط أداء المشاريع 📊

- **القيمة الحالية**: متوسط نسبة تقدم المشاريع
- **الهدف السنوي**: من إدارة التطوير (افتراضي: 90%)
- **اللون**: أزرق فاتح (info)
- **التفاصيل**: "X مشروع فوق التوقعات"

### 🔧 الحسابات

#### نسبة الفوز

```typescript
winRate = tenderStatsFromHook.winRate
```

#### إحصائيات المشاريع

```typescript
total = projects.length
active = projects.filter((p) => ['active', 'delayed', 'planning'].includes(p.status))
completed = projects.filter((p) => p.status === 'completed')
```

#### الإيرادات

```typescript
revenue = selectWonTendersValue(tenders) / 1_000_000 // بالملايين
growth = ((current - lastYear) / lastYear) * 100
```

#### الأداء

```typescript
average = sum(projects.progress) / projects.length
excellent = projects.filter((p) => p.progress >= 90)
```

### 📦 المدخلات

```typescript
{
  onSectionChange: Function // للتنقل عند النقر على البطاقة
}
```

### 🔗 التكامل

- يستخدم `useDevelopment()` للحصول على الأهداف السنوية
- يستخدم `useFinancialState()` للبيانات الفعلية
- يستخدم `useTenders()` لإحصائيات المنافسات

### ✨ المميزات

- ✅ ربط تلقائي مع الأهداف السنوية
- ✅ عرض معلومات إضافية (details)
- ✅ حسابات ديناميكية من البيانات الحقيقية
- ✅ معالجة حالات Loading
- ✅ قيم افتراضية ذكية

### ⚠️ ملاحظة مهمة

هذا المكون يبدو أنه **غير مستخدم حالياً** في لوحة التحكم الرئيسية. لوحة التحكم تستخدم `DashboardKPICards` بدلاً منه والذي يوفر قابلية التخصيص.

---

## 4️⃣ UnifiedKPICard.tsx

**📍 المسار**: `src/presentation/components/kpi/UnifiedKPICard.tsx`

### 🎯 الغرض الرئيسي

**بطاقة موحدة** قابلة لإعادة الاستخدام لعرض مؤشر أداء واحد (KPI Card).

### 🎨 تصميم البطاقة

```
┌─────────────────────────────────┐
│ [أيقونة] العنوان      [Badge]   │  ← Header
│                                 │
│ القيمة الحالية    الهدف: XX    │  ← Values
│ ──────────────────              │  ← Progress Bar
│ نسبة الإنجاز           XX%      │  ← Progress %
└─────────────────────────────────┘
```

### 📦 المدخلات (Props)

```typescript
{
  title: string              // عنوان المؤشر
  icon: ReactNode            // أيقونة (JSX)
  current: number            // القيمة الحالية
  target: number             // القيمة المستهدفة
  unit: string               // الوحدة: 'number' | 'percentage' | 'currency'
  colorClass?: string        // لون النص (Tailwind)
  bgClass?: string           // لون الخلفية (Tailwind)
  onClick?: Function         // حدث النقر
}
```

### 🎨 العناصر الرئيسية

#### 1. الأيقونة والعنوان

- أيقونة ملونة في خلفية دائرية
- عنوان قصير واضح

#### 2. Badge الحالة

يتم حسابه تلقائياً حسب النسبة:

- ≥100%: **ممتاز** 🟢 (success)
- ≥80%: **على المسار** 🔵 (info)
- ≥50%: **متوسط** 🟡 (warning)
- <50%: **يحتاج تحسين** 🔴 (destructive)

#### 3. القيم

- **القيمة الحالية**: كبيرة وملونة
- **القيمة المستهدفة**: صغيرة على اليسار

#### 4. Progress Bar

- شريط تقدم يعرض النسبة المئوية
- يتوقف عند 100% كحد أقصى
- ارتفاع: `h-1.5`

#### 5. نسبة الإنجاز

- عرض النسبة المئوية بالأسفل
- ملونة بنفس لون المؤشر

### 🔧 دوال المساعدة

#### formatValue()

تنسيق القيمة حسب الوحدة:

```typescript
'percentage' → "85%"
'number'     → "12"
'currency'   → "60"  // يتم التنسيق خارجياً
```

#### getStatus()

حساب الحالة تلقائياً:

```typescript
progress ≥100 → 'ممتاز'
progress ≥80  → 'على المسار'
progress ≥50  → 'متوسط'
progress <50  → 'يحتاج تحسين'
```

### 🎨 التفاعل

- **Hover**: تكبير الظل + تغيير حدود
- **Click**: تفعيل onClick للانتقال للصفحة المعنية
- **Animation**: انتقالات سلسة

### ✨ المميزات

- ✅ موحد وقابل لإعادة الاستخدام
- ✅ حساب تلقائي للنسبة والحالة
- ✅ تنسيق ذكي للقيم
- ✅ تصميم متجاوب
- ✅ Accessibility friendly
- ✅ معالجة القيم غير الصحيحة (target = 0)

### 🔗 الاستخدام

```tsx
<UnifiedKPICard
  title="نسبة فوز المنافسات"
  icon={<Trophy className="h-4 w-4 text-warning" />}
  current={75}
  target={80}
  unit="percentage"
  colorClass="text-warning"
  bgClass="bg-warning/10"
  onClick={() => navigate('tenders')}
/>
```

---

## 📊 مقارنة الاستخدام

| الملف                 | الموقع                    | الاستخدام الحالي | العدد    | قابلية التخصيص  |
| --------------------- | ------------------------- | ---------------- | -------- | --------------- |
| **DashboardKPICards** | لوحة التحكم الرئيسية      | ✅ نشط           | 5 بطاقات | ✅ قابل للتخصيص |
| **TenderStatusCards** | لوحة التحكم - الصف الثاني | ✅ نشط           | بطاقتين  | ❌ ثابت         |
| **AnnualKPICards**    | غير مستخدم حالياً         | ❌ غير نشط       | 4 بطاقات | ❌ ثابت         |
| **UnifiedKPICard**    | مستخدم داخلياً            | ✅ نشط           | -        | -               |

---

## 🔄 تدفق البيانات

### DashboardKPICards (قابل للتخصيص)

```
useDevelopment()       → أهداف المستخدم
   ↓
useKPIMetrics()        → بيانات النظام
   ↓
useKPIs()              → دمج وحساب
   ↓
DashboardKPICards      → عرض مخصص (5 بطاقات)
   ↓
UnifiedKPICard × 5     → بطاقات فردية
```

### AnnualKPICards (ثابت)

```
useDevelopment()       → الأهداف السنوية
useFinancialState()    → بيانات المشاريع
useTenders()           → بيانات المنافسات
   ↓
AnnualKPICards         → حسابات داخلية
   ↓
UnifiedKPICard × 4     → 4 بطاقات ثابتة
```

### TenderStatusCards (متخصص)

```
useTenderListStore()   → قائمة المنافسات
useTenders()           → إحصائيات
   ↓
TenderStatusCards      → حسابات داخلية
   ↓
Card × 2               → بطاقتين مخصصتين
```

---

## 🎯 التوصيات

### 1. DashboardKPICards

- ✅ **الأفضل للاستخدام**: قابل للتخصيص ومرن
- ✅ **يدعم**: أي عدد من الفئات
- ✅ **مناسب**: لوحة التحكم الرئيسية

### 2. AnnualKPICards

- ⚠️ **غير مستخدم حالياً**
- 💡 **يمكن استخدامه**: كصفحة منفصلة للتقارير السنوية
- 💡 **أو دمجه**: مع DashboardKPICards

### 3. TenderStatusCards

- ✅ **متخصص ومفيد**: لعرض تفاصيل المنافسات
- ✅ **يبقى كما هو**: في موقعه الحالي

### 4. UnifiedKPICard

- ✅ **مكون أساسي**: يُستخدم في جميع البطاقات
- ✅ **موحد ومرن**: يدعم جميع الحالات

---

**📅 تاريخ التقرير**: 4 نوفمبر 2025
