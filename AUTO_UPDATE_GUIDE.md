# دليل التحديثات التلقائية

## Auto-Update Guide

---

## ✅ الإعداد الحالي للتحديثات

التطبيق مُعَدّ بالكامل لاستقبال التحديثات التلقائية من GitHub Releases:

### 1. المكونات المطلوبة ✓

- ✅ **electron-updater**: مثبت (v6.6.2)
- ✅ **إعدادات البناء**: electron-builder.yml مُعَدّ صحيحاً
- ✅ **GitHub Publishing**: مُفعّل في electron-builder.yml
- ✅ **Auto-Updater Logic**: مُضمّن في src/electron/main.cjs

### 2. كيفية عمل التحديثات التلقائية

```
التطبيق يتحقق من التحديثات في الحالات التالية:
├─ عند بدء التشغيل (بعد دقيقة واحدة)
├─ كل 6 ساعات تلقائياً
└─ عند فتح قائمة "مساعدة" → "التحقق من التحديثات"
```

### 3. مسار التحديث الكامل

```
1. فحص GitHub Releases للإصدار الأحدث
   ↓
2. إذا وُجد إصدار أحدث → تحميل تلقائي في الخلفية
   ↓
3. بعد التحميل → إظهار نافذة حوار للمستخدم
   ↓
4. المستخدم يختار:
   ├─ إعادة التشغيل الآن → تثبيت وإعادة تشغيل
   └─ لاحقاً → التثبيت عند إغلاق التطبيق
```

---

## 🧪 اختبار نظام التحديثات

### الخطوة 1: إنشاء إصدار تجريبي

```bash
# 1. تحديث رقم الإصدار في package.json
"version": "1.0.2"

# 2. بناء التطبيق
npm run build:electron

# 3. النتيجة المتوقعة:
build/electron/
├── DesktopManagementSystem-1.0.2-win-x64.exe
└── latest.yml
```

### الخطوة 2: نشر على GitHub

1. اذهب إلى: https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/releases
2. اضغط "Draft a new release"
3. املأ البيانات:
   - **Tag**: `v1.0.2`
   - **Title**: `نظام إدارة شركة المقاولات v1.0.2`
   - **Description**: اكتب ملاحظات الإصدار
4. ارفع الملفات:
   - ✅ `DesktopManagementSystem-1.0.2-win-x64.exe`
   - ✅ `latest.yml` (مهم جداً!)
5. اضغط "Publish release"

### الخطوة 3: اختبار التحديث

1. شغّل التطبيق الحالي (v1.0.1)
2. انتظر دقيقة واحدة أو افتح: قائمة المساعدة → التحقق من التحديثات
3. يجب أن تظهر رسالة: "تحديث متاح v1.0.2"
4. انتظر التحميل التلقائي
5. اختر "إعادة التشغيل" للتثبيت

---

## 📊 مراقبة التحديثات

### سجلات التحديث (Audit Log)

يتم تسجيل جميع عمليات التحديث في:

```
الموقع: userData/app_security_audit_log
التصنيف: auto-updater
الأحداث المسجلة:
├─ checking-for-update
├─ update-available
├─ update-not-available
├─ update-downloaded
├─ install-requested
└─ error
```

### عرض السجلات من التطبيق

يمكنك عرض سجل التحديثات من خلال:

1. فتح التطبيق
2. الذهاب إلى الإعدادات → الأمان
3. عرض سجل المراجعة (Audit Log)
4. فلترة حسب التصنيف: "auto-updater"

---

## 🔧 استكشاف المشاكل

### المشكلة: التطبيق لا يكتشف التحديث

**الأسباب المحتملة:**

1. ❌ ملف `latest.yml` غير مرفوع على GitHub Release
2. ❌ رقم الإصدار في GitHub أقل من أو يساوي الإصدار الحالي
3. ❌ مشكلة في الاتصال بالإنترنت
4. ❌ GitHub Releases غير عام (public)

**الحل:**

```bash
# تحقق من محتوى latest.yml:
version: 1.0.2
files:
  - url: DesktopManagementSystem-1.0.2-win-x64.exe
    sha512: [hash]
    size: [bytes]
path: DesktopManagementSystem-1.0.2-win-x64.exe
sha512: [hash]
releaseDate: '2025-10-29T...'
```

### المشكلة: التحديث يفشل أثناء التثبيت

**الأسباب:**

1. ❌ صلاحيات غير كافية (يحتاج admin)
2. ❌ برنامج حماية يمنع التثبيت
3. ❌ ملف exe تالف أثناء الرفع

**الحل:**

- تشغيل التطبيق كمسؤول عند التثبيت
- إضافة استثناء في برنامج الحماية
- إعادة رفع الملف على GitHub

---

## 📝 ملاحظات مهمة

### 1. ترقيم الإصدارات (Semantic Versioning)

```
MAJOR.MINOR.PATCH
  ↓     ↓     ↓
1  .  0  .  1

- MAJOR: تغييرات كبيرة غير متوافقة
- MINOR: إضافات وميزات جديدة متوافقة
- PATCH: إصلاحات وتحسينات صغيرة
```

### 2. ملف latest.yml

- يُنشأ تلقائياً من electron-builder
- **يجب** رفعه مع كل إصدار على GitHub
- يحتوي على معلومات التحديث (الحجم، التاريخ، hash)

### 3. التوقيع الرقمي (Code Signing)

حالياً: `forceCodeSigning: false`

لإضافة التوقيع الرقمي مستقبلاً:

```yaml
win:
  certificateFile: path/to/certificate.pfx
  certificatePassword: ${env.CERTIFICATE_PASSWORD}
  forceCodeSigning: true
```

---

## 🎯 الخطوات التالية الموصى بها

### للمطورين:

1. ✅ إضافة description و author في package.json (تم)
2. ✅ إعداد auto-updater (تم)
3. 🔲 إنشاء نظام للتحقق من صحة التحديث قبل التثبيت
4. 🔲 إضافة progress bar لتحميل التحديث
5. 🔲 إضافة changelog عربي/إنجليزي لكل إصدار

### للإنتاج:

1. 🔲 إنشاء pipeline CI/CD للبناء التلقائي
2. 🔲 إضافة code signing certificate
3. 🔲 إعداد staged rollout (إطلاق تدريجي)
4. 🔲 مراقبة معدلات النجاح/الفشل للتحديثات

---

## 📞 الدعم الفني

إذا واجهت مشاكل:

1. تحقق من سجل الأخطاء: `%AppData%/نظام إدارة شركة المقاولات/error-logs/`
2. راجع سجل المراجعة داخل التطبيق (auto-updater events)
3. تواصل مع فريق التطوير مع إرفاق:
   - رقم الإصدار الحالي
   - رسالة الخطأ
   - ملف السجل

---

**آخر تحديث**: 29 أكتوبر 2025  
**الإصدار المستهدف**: v1.0.1+
