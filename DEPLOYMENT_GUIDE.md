# دليل تجهيز التطبيق للنشر

## Desktop Management System - Community Edition

**الإصدار المستهدف:** 1.0.0
**تاريخ الإنشاء:** 2025-10-29

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات الأساسية](#المتطلبات-الأساسية)
3. [المرحلة 1: إصلاح الأخطاء الحرجة](#المرحلة-1-إصلاح-الأخطاء-الحرجة)
4. [المرحلة 2: التحسين والتنظيف](#المرحلة-2-التحسين-والتنظيف)
5. [المرحلة 3: الاختبار](#المرحلة-3-الاختبار)
6. [المرحلة 4: النشر](#المرحلة-4-النشر)
7. [الصيانة والدعم](#الصيانة-والدعم)

---

## 🎯 نظرة عامة

هذا الدليل يوفر خطة مفصلة خطوة بخطوة لتجهيز التطبيق للنشر الإنتاجي.

**الحالة الحالية:**

- ✅ التطبيق يعمل في بيئة التطوير
- ⚠️ يحتوي على 2,683 خطأ TypeScript
- ⚠️ يحتاج تنظيف وتحسين
- ⚠️ يحتاج اختبار شامل

**الهدف النهائي:**

- ✅ 0 أخطاء TypeScript
- ✅ جميع الاختبارات تنجح
- ✅ توثيق كامل
- ✅ جاهز للنشر الإنتاجي

---

## 🔧 المتطلبات الأساسية

### البرامج المطلوبة

```bash
# Node.js >= 18.0.0
node --version

# npm >= 9.0.0
npm --version

# Git
git --version
```

### تثبيت التبعيات

```bash
cd "path/to/project"
npm install
```

### التحقق من البيئة

```bash
# فحص TypeScript
npx tsc --version

# فحص Electron
npx electron --version

# فحص Vite
npx vite --version
```

---

## 🚀 المرحلة 1: إصلاح الأخطاء الحرجة

### الخطوة 1.1: تحليل الأخطاء

```bash
# فحص جميع أخطاء TypeScript وحفظها في ملف
npx tsc --noEmit > typescript-errors.txt 2>&1

# عرض ملخص الأخطاء
cat typescript-errors.txt | grep "error TS" | wc -l
```

### الخطوة 1.2: تصنيف الأخطاء

قم بتصنيف الأخطاء حسب الأولوية:

**أولوية عالية (يجب إصلاحها أولاً):**

1. استيرادات مفقودة (Cannot find module)
2. واجهات غير مكتملة (Property does not exist)
3. تعارضات أنواع حرجة (Type is not assignable)

**أولوية متوسطة:**

1. وظائف مفقودة أو محذوفة
2. تعارضات أنواع بسيطة
3. مشاكل في الاختبارات

**أولوية منخفضة:**

1. متغيرات غير مستخدمة (is declared but never read)
2. معاملات ضمنية (implicitly has 'any' type)
3. خصائص محددة مرتين (specified more than once)

### الخطوة 1.3: إصلاح الاستيرادات المفقودة

#### مثال: إصلاح `@/utils/storageSchema`

```bash
# فحص المسارات الموجودة
find src -name "*storage*" -type f

# إذا كان الملف موجوداً بمسار مختلف، قم بتحديث الاستيراد
# إذا كان مفقوداً، قم بإنشائه أو إزالة الاستيراد
```

**الملفات التي تحتاج إصلاح:**

1. `scripts/migrations/cleanup-tender-wizards.ts`

   - حذف أو تعديل استيراد `sanitizeTenderPricingWizardStore`

2. `src/application/services/bidComparisonService.ts`

   - تحديث المسار من `../utils/storage` إلى المسار الصحيح

3. `tests/unit/stores/projectStore.test.ts`
   - التحقق من وجود `@/application/stores/projectStore`

### الخطوة 1.4: إصلاح الواجهات غير المكتملة

#### مثال: إصلاح WebhookDelivery

**الموقع:** `src/api/integrations/webhookService.ts`

```typescript
// قبل الإصلاح
interface WebhookDelivery {
  id: string
  webhookId: string
  status: 'pending' | 'success' | 'failed'
  timestamp: Date
}

// بعد الإصلاح
interface WebhookDelivery {
  id: string
  webhookId: string
  status: 'pending' | 'success' | 'failed' | 'retrying' | 'delivered'
  timestamp: Date
  statusCode?: number
  deliveredAt?: Date
  createdAt?: Date
  error?: string
}
```

**قائمة الواجهات التي تحتاج تحديث:**

1. `WebhookDelivery` - إضافة الخصائص المفقودة
2. `WebhookPayload` - إضافة خاصية `source`
3. `MarketTrend` - إضافة `timeline`, `segment`, `createdAt`, `updatedAt`
4. `MarketOpportunity` - إضافة `timeline`, `region`, `estimatedValue`
5. `CompetitorData` - إضافة `lastUpdated`, `region`, `categories`, `type`

### الخطوة 1.5: إصلاح الوظائف المفقودة

#### مثال: إصلاح useEditableTenderPricing

**الموقع:** `src/application/hooks/useEditableTenderPricing.ts`

الخيار 1: إضافة الوظائف المفقودة

```typescript
// في src/services/pricingSnapshot.ts
export const pricingSnapshot = {
  // ... existing functions

  loadOfficial: async (tenderId: string) => {
    // Implementation
  },

  loadDraft: async (tenderId: string) => {
    // Implementation
  },

  saveOfficial: async (tenderId: string, data: any) => {
    // Implementation
  },

  saveDraft: async (tenderId: string, data: any) => {
    // Implementation
  },
}
```

الخيار 2: تحديث الكود ليستخدم API الحالي

```typescript
// في useEditableTenderPricing.ts
// استبدل
const official = await pricingSnapshot.loadOfficial(tenderId)
// بـ
const official = await pricingSnapshot.load(tenderId)
```

---

## 🧹 المرحلة 2: التحسين والتنظيف

### الخطوة 2.1: إزالة المتغيرات غير المستخدمة

```bash
# تشغيل ESLint مع الإصلاح التلقائي
npm run lint:fix

# أو يدوياً
npx eslint "src/**/*.{ts,tsx}" --fix
```

### الخطوة 2.2: تحديث tsconfig.json

قد تحتاج لتخفيف بعض القيود مؤقتاً:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": false, // تعطيل مؤقتاً
    "noUnusedParameters": false // تعطيل مؤقتاً
    // ... rest of config
  }
}
```

**ملاحظة:** أعد تفعيلها بعد التنظيف!

### الخطوة 2.3: تحديث الاختبارات

```bash
# تشغيل جميع الاختبارات
npm run test

# عرض الاختبارات الفاشلة
npm run test 2>&1 | grep "FAIL"
```

**الملفات التي تحتاج تحديث:**

1. `tests/unit/services/unifiedSystemIntegrationService.test.ts`

   - إزالة/تحديث الاختبارات للوظائف المحذوفة

2. `tests/utils/predictionModels.test.ts`
   - تصدير الوظائف المطلوبة أو تحديث الاختبارات

### الخطوة 2.4: تنظيف الملفات المؤقتة

```bash
# حذف ملفات البناء القديمة
rm -rf dist
rm -rf build/electron

# حذف node_modules وإعادة التثبيت (اختياري)
rm -rf node_modules
npm install
```

---

## 🧪 المرحلة 3: الاختبار

### الخطوة 3.1: اختبار TypeScript

```bash
# يجب أن يعيد 0 أخطاء
npx tsc --noEmit
echo $?  # يجب أن يكون 0
```

### الخطوة 3.2: اختبار الوحدات

```bash
# تشغيل جميع الاختبارات
npm run test

# تشغيل اختبارات معينة
npm run test:unit

# مع تقرير التغطية
npm run test:coverage
```

### الخطوة 3.3: اختبار البناء

```bash
# بناء التطبيق
npm run build

# التحقق من نجاح البناء
ls -lh dist

# اختبار الـ Preview
npm run preview
```

### الخطوة 3.4: اختبار Electron

```bash
# بناء Electron
npm run build:electron

# التحقق من الملفات الناتجة
ls -lh build/electron/

# اختبار التطبيق يدوياً
# افتح الملف التنفيذي من build/electron/win-unpacked/
```

### الخطوة 3.5: اختبار E2E

```bash
# تشغيل اختبارات Playwright
npm run test:e2e:desktop
```

---

## 📦 المرحلة 4: النشر

### الخطوة 4.1: تحديث معلومات الإصدار

#### تحديث package.json

```json
{
  "name": "desktop-management-system-community",
  "version": "1.0.0", // من 0.1.0 إلى 1.0.0
  "description": "نظام إدارة شركة المقاولات - Community Edition",
  "author": {
    "name": "Your Name",
    "email": "your.email@example.com"
  }
  // ... rest
}
```

#### إنشاء CHANGELOG.md

```markdown
# Changelog

## [1.0.0] - 2025-XX-XX

### Added

- نظام إدارة المنافسات
- نظام إدارة المشاريع
- نظام التسعير الموحد
- نظام إدارة العملاء
- تقارير وتحليلات متقدمة

### Fixed

- إصلاح 2,683 خطأ TypeScript
- تحسين الأداء والاستقرار
- إصلاح مشاكل الاستيرادات

### Changed

- تحديث البنية المعمارية
- تحسين واجهة المستخدم
```

### الخطوة 4.2: إعداد ملفات البيئة

#### إنشاء .env.production

```bash
cp .env.example .env.production
```

```env
# .env.production
NODE_ENV=production

# Sentry Configuration
SENTRY_DSN=your-sentry-dsn-here
SENTRY_ENVIRONMENT=production

# Feature Flags
VITE_FEATURE_DOMAIN_PRICING_UI=true
VITE_FEATURE_PRICING_DUAL_WRITE=true
VITE_FEATURE_ENGINE_AUTHORING=true

# App Configuration
VITE_APP_VERSION=1.0.0
VITE_APP_NAME="نظام إدارة شركة المقاولات"
```

### الخطوة 4.3: تحديث electron-builder.yml

```yaml
appId: com.mbm.desktop.management
productName: 'نظام إدارة شركة المقاولات'

# تحديث معلومات النشر
publish:
  - provider: github
    owner: your-username
    repo: your-repo-name
    releaseType: release
    token: ${GH_TOKEN} # تأكد من وجود token

# تحديث الأيقونة والموارد
win:
  icon: build/resources/icon.ico
  target:
    - target: nsis
      arch:
        - x64
```

### الخطوة 4.4: إنشاء Release Notes

```markdown
# Desktop Management System v1.0.0

## 🎉 الإصدار الأول - Production Ready

### الميزات الرئيسية

- ✅ نظام إدارة المنافسات الكامل
- ✅ نظام إدارة المشاريع
- ✅ نظام التسعير الموحد والمتقدم
- ✅ إدارة العملاء والموردين
- ✅ تقارير وتحليلات شاملة
- ✅ واجهة عربية كاملة
- ✅ دعم التحديث التلقائي

### المتطلبات

- Windows 10/11 (64-bit)
- 4GB RAM (موصى به 8GB)
- 500MB مساحة تخزين
- .NET Framework 4.7.2 أو أحدث

### التثبيت

1. قم بتحميل الملف: `DesktopManagementSystem-1.0.0-win-x64.exe`
2. قم بتشغيل الملف واتبع التعليمات
3. قم بتشغيل التطبيق من سطح المكتب أو قائمة ابدأ

### الترقية من الإصدارات السابقة

- النسخ الاحتياطية ستُنقل تلقائياً
- يُنصح بعمل نسخة احتياطية يدوية قبل الترقية

### المشاكل المعروفة

- لا توجد مشاكل معروفة في هذا الإصدار

### الدعم

- لأي مشاكل أو استفسارات، قم بفتح Issue على GitHub
- البريد الإلكتروني: support@example.com
```

### الخطوة 4.5: البناء النهائي

```bash
# تنظيف البناء السابق
rm -rf dist build/electron

# البناء النهائي
npm run build:electron

# التحقق من الملفات الناتجة
ls -lh build/electron/

# الملفات المتوقعة:
# - DesktopManagementSystem-1.0.0-win-x64.exe (المثبت)
# - latest.yml (معلومات التحديث)
```

### الخطوة 4.6: اختبار المثبت

1. **اختبار على جهاز نظيف:**

   - قم بتثبيت التطبيق على جهاز نظيف (VM أو جهاز اختبار)
   - تأكد من عمل جميع الميزات
   - تأكد من عدم وجود أخطاء

2. **اختبار الترقية:**

   - ثبّت نسخة قديمة
   - ثبّت النسخة الجديدة فوقها
   - تأكد من انتقال البيانات بشكل صحيح

3. **اختبار إلغاء التثبيت:**
   - قم بإلغاء تثبيت التطبيق
   - تأكد من حذف جميع الملفات (أو الاحتفاظ بالبيانات حسب الإعداد)

### الخطوة 4.7: النشر على GitHub

```bash
# إنشاء Git Tag
git tag -a v1.0.0 -m "Release v1.0.0 - Production Ready"
git push origin v1.0.0

# استخدام GitHub CLI (اختياري)
gh release create v1.0.0 \
  build/electron/*.exe \
  build/electron/latest.yml \
  --title "Desktop Management System v1.0.0" \
  --notes-file RELEASE_NOTES.md

# أو قم بالرفع يدوياً عبر واجهة GitHub
```

### الخطوة 4.8: تفعيل Auto-Updates

تأكد من:

1. ✅ ملف `latest.yml` موجود في الـ Release
2. ✅ الـ URL في `electron-builder.yml` صحيح
3. ✅ GitHub Token له صلاحيات القراءة

---

## 🔄 الصيانة والدعم

### المراقبة

#### Sentry للأخطاء

```bash
# تحقق من Sentry Dashboard بانتظام
# https://sentry.io/organizations/your-org/issues/
```

#### تتبع الاستخدام

- راقب عدد التنزيلات
- راقب التحديثات التلقائية
- راقب تقارير الأخطاء

### التحديثات الدورية

#### تحديثات الأمان

```bash
# فحص الثغرات الأمنية
npm audit

# إصلاح تلقائي
npm audit fix

# تحديث التبعيات
npm update

# تحديث major versions (بحذر)
npx npm-check-updates -u
npm install
```

#### تحديثات الميزات

**كل 2-4 أسابيع:**

1. جمع ملاحظات المستخدمين
2. إصلاح Bugs المبلغ عنها
3. إضافة تحسينات صغيرة
4. نشر تحديث minor (1.1.0, 1.2.0, etc.)

**كل 3-6 أشهر:**

1. مراجعة شاملة
2. إضافة ميزات كبيرة
3. تحسينات معمارية
4. نشر تحديث major (2.0.0, 3.0.0, etc.)

### خطة الدعم

#### القنوات

1. **GitHub Issues** - لتتبع Bugs والميزات المطلوبة
2. **البريد الإلكتروني** - للدعم المباشر
3. **التوثيق** - FAQ و User Guides

#### أوقات الاستجابة

- Bugs حرجة: 24 ساعة
- Bugs عادية: 3-5 أيام
- طلبات ميزات: حسب الأولوية

---

## 📊 مؤشرات الأداء

### مؤشرات فنية

- ✅ 0 أخطاء TypeScript
- ✅ Test Coverage > 60%
- ✅ Build Success Rate: 100%
- ✅ No Critical Security Issues

### مؤشرات تجربة المستخدم

- ✅ وقت التحميل < 5 ثواني
- ✅ استهلاك الذاكرة < 500MB
- ✅ حجم المثبت < 150MB
- ✅ معدل نجاح التثبيت > 95%

### مؤشرات الأعمال

- 📈 عدد التنزيلات
- 📈 معدل الاحتفاظ بالمستخدمين
- 📈 رضا المستخدمين
- 📉 معدل الأخطاء

---

## ✅ قائمة التحقق النهائية

قبل النشر الإنتاجي، تأكد من:

### الكود

- [ ] 0 أخطاء TypeScript
- [ ] جميع الاختبارات تنجح
- [ ] ESLint بدون أخطاء
- [ ] لا توجد console.log في الكود الإنتاجي
- [ ] جميع TODO مُعالجة أو موثقة

### التوثيق

- [ ] README.md محدث
- [ ] CHANGELOG.md محدث
- [ ] DEPLOYMENT_READINESS_REPORT.md محدث
- [ ] API Documentation كامل
- [ ] User Guide متوفر

### الأمان

- [ ] جميع المفاتيح السرية في .env
- [ ] لا توجد مفاتيح مكشوفة في Git
- [ ] CSP مُعد بشكل صحيح
- [ ] npm audit بدون ثغرات حرجة
- [ ] Code Signing مُفعل (إن أمكن)

### البناء والنشر

- [ ] Build يعمل بدون أخطاء
- [ ] المثبت يعمل بشكل صحيح
- [ ] Auto-Updates يعمل
- [ ] الحجم مقبول (< 150MB)
- [ ] اختبار على أجهزة متعددة

### التجربة

- [ ] جميع الميزات تعمل
- [ ] لا يوجد تجمد أو بطء
- [ ] الواجهة سلسة وسريعة
- [ ] الرسائل واضحة ومفهومة
- [ ] لا توجد أخطاء واضحة

### الدعم

- [ ] قنوات الدعم جاهزة
- [ ] خطة الصيانة موثقة
- [ ] Monitoring مُفعل (Sentry)
- [ ] Backup strategy موضوعة

---

## 🎓 موارد إضافية

### توثيق التقنيات المستخدمة

- [Electron Documentation](https://www.electronjs.org/docs)
- [Vite Documentation](https://vitejs.dev/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Vitest Documentation](https://vitest.dev/)

### أفضل الممارسات

- [Electron Security](https://www.electronjs.org/docs/latest/tutorial/security)
- [TypeScript Best Practices](https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html)
- [Testing Best Practices](https://testingjavascript.com/)

### الدعم والمجتمع

- [GitHub Issues](https://github.com/your-repo/issues)
- [Electron Discord](https://discord.gg/electron)
- [Stack Overflow](https://stackoverflow.com/questions/tagged/electron)

---

**تم إنشاء الدليل بواسطة:** Claude Code
**آخر تحديث:** 2025-10-29
**الإصدار:** 1.0
