# 🏗️ نظام إدارة شركة المقاولات المتطور

## نظرة عامة

نظام مكتبي يعتمد على React و Electron لإدارة أعمال شركات المقاولات باللغة العربية مع دعم كامل للاتجاه من اليمين إلى اليسار ووضع ليلي/نهاري.

## المميزات الرئيسية

- واجهة مكتبية متجاوبة مبنية بـ React 18.3.1 و TypeScript مع تجزئة مكونات واضحة.
- تخزين محلي موحّد عبر electron-store مع طبقة حماية (`contextIsolation`) وواجهات IPC مضبوطة في `preload.cjs`.
- دعم RTL كامل وخطوط عربية جاهزة باستخدام TailwindCSS 3.4 وطبقة تصميم موحدة.
- لوحات تحكم ورسوم بيانية غنية باستخدام Recharts و Shadcn UI، مع نماذج تفاعلية مبنية على `react-hook-form`.
- نظام نماذج ووحدات بيانات لإدارة المشاريع والمناقصات والعملاء والمالية، مدعوم بخدمات تطبيقية (`application/services`).
- بنية دومينية لإدارة التسعير وBOQ عبر مجلد `domain/` مع مراقبة مدمجة.

## التشغيل السريع

### المتطلبات

- Node.js 18 أو أحدث.
- npm 9 أو أحدث.

### خطوات التشغيل

```bash
npm install
npm run dev      # تشغيل Vite في وضع التطوير
npm run dev:electron  # تشغيل الغلاف المكتبي
```

### البناء للإنتاج

```bash
npm run build
npm run build:electron
```

## هيكل المجلدات (مختصر)

```text
src/
  application/     # الخدمات وطبقة التطبيق (Hooks, Services)
  components/      # مكونات الواجهة وشاشات النظام
  domain/          # نماذج التسعير والمنطق الدوميني
  electron/        # ملفات main & preload الخاصة بـ Electron
  utils/           # الأدوات المساعدة (تخزين، حسابات...)
tests/             # اختبارات Vitest للوحدات وطبقة الدومين
docs/              # مستندات هندسية وخطة التحسين
scripts/           # سكربتات الأتمتة وعمليات الترحيل
```

## التقنيات الأساسية

| الفئة | المكتبة/الأداة | الإصدار الحالي |
| --- | --- | --- |
| إطار الواجهة | React | 18.3.1 |
| التغليف المكتبي | Electron | 38.0.0 |
| الأدوات | Vite | 5.3.5 |
| واجهات UI | shadcn/ui + Radix UI | 2025-09 snapshot |
| الأنماط | TailwindCSS | 3.4.10 |
| التخزين المحلي | electron-store | 10.1.0 |
| الاختبارات | Vitest | 1.6.0 |

## بنية التطبيق (طبقات)

1. **طبقة الواجهة (UI Layer):** مكونات React موزعة في `components/` مع اعتماد على تصميم عربي ودعم كامل لـ RTL.
2. **طبقة التطبيق (Application Layer):** Hooks وخدمات تحت `application/` توفر عمليات CRUD، سياق التنقل، وإدارة الحالة مع أحداث النظام.
3. **طبقة الدومين (Domain Layer):** نماذج الكميات والتسعير في `domain/`، تشمل عمليات مراقبة الأداء ومعادلات BOQ.
4. **طبقة البنية التحتية (Infrastructure):** التخزين، التهيئة، والمرافق في `utils/` و`config/`, بالإضافة إلى ملفات Electron (`main.cjs` و`preload.cjs`).

## سكربتات npm المهمة

```bash
npm run dev            # تشغيل Vite للتطوير
npm run dev:electron  # تشغيل التطبيق كبرنامج سطح مكتب مع Electron
npm run build         # بناء نسخة الإنتاج للواجهة
npm run build:electron # بناء الحزم التنفيذية عبر electron-builder
npm run test          # تشغيل اختبارات Vitest
```

## خارطة التحسين (مختصرة)

- المرحلة الأولى: توثيق الخطة، تفكيك حالة التنقل، تجهيز أدوات الفحص البرمجي.
- المرحلة الثانية: فصل خدمة البيانات المركزية إلى مستودعات وحقن اعتمادات.
- المرحلة الثالثة: تشديد الأمان لقنوات IPC وتحسين سياسات الأمان والتحديث.
- المرحلة الرابعة: رفع تغطية الاختبارات وتحسين تجربة المستخدم وإتاحة لغات إضافية.

## الدعم والتواصل

- البريد: [support@construction-system.com](mailto:support@construction-system.com)
- المجتمع: [Discord](https://discord.gg/construction-system)
- الوثائق: [docs.construction-system.com](https://docs.construction-system.com)

## الترخيص

هذا المشروع مرخص تحت [رخصة MIT](LICENSE).
