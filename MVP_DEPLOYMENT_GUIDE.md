# دليل نشر MVP - Desktop Management System

## MVP Deployment Guide - Production Ready with Best Practices

**الإصدار:** MVP 1.0.0
**تاريخ الإنشاء:** 2025-10-29
**الحالة:** ✅ جاهز للنشر مع ضمانات حماية كاملة

---

## 🎯 ملخص تنفيذي

هذا الدليل يُركز فقط على الأنظمة الأساسية للـ MVP:

1. **تسجيل الأخطاء وإرسالها إلى GitHub** ✅
2. **استقبال التحديثات التلقائية** ✅
3. **حماية البيانات المحلية من الفقدان** ✅
4. **الحفاظ على البيانات عند الترقية** ✅

**النتيجة:** جميع الأنظمة موجودة وتعمل! يحتاج فقط إعدادات بسيطة.

---

## ✅ تقرير الفحص - جميع الأنظمة جاهزة!

### 1. نظام تسجيل الأخطاء ✅ متوفر وجاهز

**الملف:** [`src/electron/errorReporter.cjs`](src/electron/errorReporter.cjs)

**الميزات:**

- ✅ تسجيل الأخطاء محلياً في ملفات JSON
- ✅ إرسال دوري كل 6 ساعات إلى GitHub Issues
- ✅ تجميع الأخطاء المتشابهة
- ✅ تنظيف تلقائي للسجلات القديمة
- ✅ معلومات نظام كاملة (Platform, Version, Memory, etc.)
- ✅ Retry mechanism للأخطاء الفاشلة

**الإعدادات الحالية:**

```javascript
const GITHUB_CONFIG = {
  owner: 'anmar534',
  repo: 'Desktop-Management-System--Community---Copy---Copy-',
  token: process.env.GITHUB_ERROR_REPORT_TOKEN || '',
  enabled: process.env.ENABLE_ERROR_REPORTING !== 'false',
}

const ERROR_LOG_CONFIG = {
  maxLogSize: 5 * 1024 * 1024, // 5 MB
  maxLogsToKeep: 10, // آخر 10 ملفات
  reportingInterval: 6 * 60 * 60 * 1000, // كل 6 ساعات
  batchSize: 50, // 50 خطأ في المرة
}
```

**كيف يعمل:**

1. عند حدوث خطأ → يُسجل في `userData/error-logs/`
2. كل 6 ساعات → يُرسل إلى GitHub Issues تلقائياً
3. GitHub Issue يحتوي على:
   - عدد مرات الحدوث
   - Stack Trace
   - معلومات النظام
   - Platform و Version

### 2. نظام التحديثات التلقائية ✅ متوفر وجاهز

**الملف:** [`src/electron/main.cjs`](src/electron/main.cjs:1526-1641)

**الميزات:**

- ✅ فحص تلقائي كل 6 ساعات
- ✅ تنزيل التحديثات في الخلفية
- ✅ إشعارات للمستخدم
- ✅ تثبيت عند إغلاق التطبيق
- ✅ Audit Log كامل للتحديثات
- ✅ دعم GitHub Releases

**الإعدادات الحالية:**

```javascript
autoUpdater.autoDownload = true // تنزيل تلقائي
autoUpdater.autoInstallOnAppQuit = true // تثبيت عند الإغلاق
const SECURITY_UPDATE_CHECK_INTERVAL_MS = 6 * 60 * 60 * 1000 // كل 6 ساعات
```

**electron-builder.yml:**

```yaml
publish:
  - provider: github
    owner: anmar534
    repo: Desktop-Management-System--Community---Copy---Copy-
    releaseType: release
```

**كيف يعمل:**

1. عند البدء → فحص فوري للتحديثات
2. كل 6 ساعات → فحص دوري
3. عند وجود تحديث → تنزيل في الخلفية
4. بعد التنزيل → إشعار للمستخدم
5. عند الإغلاق → تثبيت تلقائي

### 3. نظام حماية البيانات المحلية ✅ متقدم جداً

**الملفات الرئيسية:**

- [`src/shared/utils/storage/backupManager.ts`](src/shared/utils/storage/backupManager.ts)
- [`src/infrastructure/storage/modules/BackupStorage.ts`](src/infrastructure/storage/modules/BackupStorage.ts)
- [`src/infrastructure/storage/core/StorageManager.ts`](src/infrastructure/storage/core/StorageManager.ts)

**الميزات:**

- ✅ **electron-store**: تخزين محلي آمن
- ✅ **Secure Storage**: تشفير AES-256-GCM مع keytar
- ✅ **نظام نسخ احتياطي تلقائي**: مصفوفة الاحتفاظ 10×30
  - آخر 10 نسخ احتياطية
  - مدة 30 يوم
- ✅ **Migration System**: ترحيل البيانات القديمة
- ✅ **Audit Log**: سجل كامل لجميع العمليات
- ✅ **Retention Policies**: سياسات حفظ ذكية

**مسارات التخزين:**

```javascript
// Windows
C:\Users\<username>\AppData\Roaming\DesktopManagementSystem\
├── config.json           // electron-store
├── error-logs/          // سجلات الأخطاء
├── backups/            // النسخ الاحتياطية
└── Cache/              // الكاش

// Secure Storage (keytar)
Windows Credential Manager → DesktopManagementSystem
```

**حماية من الفقدان:**

1. **تخزين محلي مزدوج:**

   - electron-store (عادي)
   - secure-store (مشفر)

2. **نسخ احتياطي تلقائي:**

   - كل عملية حفظ → نسخة احتياطية
   - مصفوفة 10×30 (آخر 10 نسخ، مدة 30 يوم)
   - تنظيف تلقائي للقديم

3. **Audit Log:**
   - تتبع جميع العمليات
   - يمكن استعادة البيانات من السجلات

### 4. استراتيجية الترقية الآمنة ✅ مُطبقة

**الأمان عند الترقية:**

1. **حفظ userData في مكان ثابت:**

```javascript
// في main.cjs
function setupSafePaths() {
  const safeAppName = resolveScopedAppName()
  const userDataPath = path.join(app.getPath('appData'), safeAppName)
  app.setPath('userData', userDataPath)
}
```

2. **Migration System:**

```typescript
// في backupManager.ts
- migrateLegacyValues → secure
- normalizeStore → تحديث البنية
- retentionPolicy → حفظ أهم البيانات
```

3. **عند التحديث:**

```javascript
// التسلسل:
1. قبل الإغلاق → حفظ جميع البيانات
2. إنشاء نسخة احتياطية
3. إغلاق التطبيق
4. تثبيت التحديث
5. فتح النسخة الجديدة
6. تحميل البيانات من نفس المسار
7. ترحيل إذا لزم (Migration)
```

---

## 🚀 خطوات النشر - MVP Ready

### المرحلة 1: الإعدادات الأساسية (5 دقائق)

#### 1.1 إنشاء GitHub Personal Access Token

```bash
# 1. اذهب إلى GitHub
https://github.com/settings/tokens/new

# 2. أنشئ Token جديد بالصلاحيات:
☑ repo (full access)
☑ public_repo (إذا كان عام)

# 3. احفظ Token (لن يظهر مرة أخرى!)
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### 1.2 إنشاء ملف .env.production

```bash
cd "path/to/your/project"
```

إنشاء الملف:

```env
# .env.production

# ===== نظام تسجيل الأخطاء =====
# GitHub Token لإنشاء Issues
GITHUB_ERROR_REPORT_TOKEN=ghp_your_token_here

# تفعيل/تعطيل الإرسال التلقائي
ENABLE_ERROR_REPORTING=true

# ===== نظام التحديثات =====
# سيتم قراءة إعدادات GitHub من electron-builder.yml

# ===== إعدادات التطبيق =====
NODE_ENV=production
VITE_APP_VERSION=1.0.0
VITE_APP_NAME="نظام إدارة شركة المقاولات"

# ===== Sentry (اختياري) =====
# SENTRY_DSN=your_sentry_dsn_here
# SENTRY_ENVIRONMENT=production
```

#### 1.3 تحديث package.json

```json
{
  "version": "1.0.0",
  "description": "نظام إدارة شركة المقاولات - Community Edition",
  "author": {
    "name": "Your Name",
    "email": "your.email@example.com"
  }
}
```

#### 1.4 تحديث electron-builder.yml (تأكد من الإعدادات)

```yaml
appId: com.mbm.desktop.management
productName: 'نظام إدارة شركة المقاولات'

publish:
  - provider: github
    owner: anmar534 # ← تأكد من هذا
    repo: Desktop-Management-System--Community---Copy---Copy- # ← تأكد من هذا
    releaseType: release
```

---

### المرحلة 2: البناء النهائي (10 دقائق)

#### 2.1 تنظيف وإعادة البناء

```bash
# تنظيف
rm -rf dist build/electron node_modules

# إعادة التثبيت
npm install

# البناء النهائي للإنتاج
cross-env NODE_ENV=production npm run build:electron
```

#### 2.2 التحقق من الملفات الناتجة

```bash
ls -lh build/electron/

# يجب أن تجد:
# - DesktopManagementSystem-1.0.0-win-x64.exe (المثبت)
# - latest.yml (معلومات التحديث)
```

---

### المرحلة 3: اختبار محلي (15 دقيقة)

#### 3.1 اختبار المثبت

```bash
# 1. افتح المثبت
build/electron/DesktopManagementSystem-1.0.0-win-x64.exe

# 2. ثبّت في مجلد اختبار
# 3. شغّل التطبيق
# 4. افتح DevTools (Ctrl+Shift+I)
```

#### 3.2 اختبار تسجيل الأخطاء

```javascript
// في DevTools Console
// اختبر تسجيل خطأ
window.electronAPI.logError(
  {
    name: 'TestError',
    message: 'This is a test error',
    stack: 'Test stack trace',
  },
  { source: 'manual-test' },
)

// تحقق من إنشاء الملف
// Windows: C:\Users\<username>\AppData\Roaming\DesktopManagementSystem\error-logs\
```

#### 3.3 اختبار الحفظ والاستعادة

```bash
# 1. أنشئ بيانات اختبار (منافسة، مشروع، etc.)
# 2. أغلق التطبيق
# 3. أعد فتحه
# 4. تأكد من وجود جميع البيانات ✅
```

#### 3.4 اختبار النسخ الاحتياطي

```javascript
// في DevTools Console
// استخرج تقرير النسخ الاحتياطية
const backups = await window.electronAPI.store.get('__backup_store__')
console.log('Backups:', backups)

// أو استخدم IPC
window.electronAPI.invoke('error-reporter-stats').then(console.log)
```

---

### المرحلة 4: النشر على GitHub (10 دقائق)

#### 4.1 إنشاء Git Tag

```bash
git add .
git commit -m "release: MVP v1.0.0 - Production Ready"
git tag -a v1.0.0 -m "MVP Release v1.0.0

✅ Error reporting to GitHub Issues
✅ Auto-updates support
✅ Local data protection
✅ Backup system (10×30 matrix)
✅ Secure storage with encryption
✅ Migration support"

git push origin v1.0.0
```

#### 4.2 إنشاء GitHub Release

**خيار 1: باستخدام GitHub CLI**

```bash
gh release create v1.0.0 \
  build/electron/*.exe \
  build/electron/latest.yml \
  --title "Desktop Management System v1.0.0 - MVP" \
  --notes-file RELEASE_NOTES.md
```

**خيار 2: يدوياً عبر الواجهة**

1. اذهب إلى `https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/releases/new`
2. اختر tag: `v1.0.0`
3. ارفع الملفات:
   - `DesktopManagementSystem-1.0.0-win-x64.exe`
   - `latest.yml`
4. أضف Release Notes (انظر النموذج أدناه)
5. انشر Release

**نموذج Release Notes:**

```markdown
# 🎉 Desktop Management System v1.0.0 - MVP Release

## ✨ الميزات الرئيسية

### 🔒 الأمان والموثوقية

- ✅ تسجيل الأخطاء تلقائياً وإرسالها إلى GitHub Issues
- ✅ تحديثات تلقائية آمنة
- ✅ تشفير البيانات الحساسة (AES-256-GCM)
- ✅ نظام نسخ احتياطي متقدم (مصفوفة 10×30)

### 📊 إدارة الأعمال

- ✅ نظام المنافسات الكامل
- ✅ إدارة المشاريع
- ✅ نظام التسعير الموحد
- ✅ إدارة العملاء
- ✅ تقارير شاملة

### 🛡️ حماية البيانات

- ✅ حفظ محلي آمن (electron-store)
- ✅ تخزين مشفر (keytar + Windows Credential Manager)
- ✅ نسخ احتياطي تلقائي
- ✅ الحفاظ على البيانات عند الترقية
- ✅ Migration System

## 📥 التثبيت

1. قم بتحميل: `DesktopManagementSystem-1.0.0-win-x64.exe`
2. قم بتشغيل المثبت
3. اتبع التعليمات
4. استمتع!

## 🔄 التحديثات التلقائية

- ✅ فحص تلقائي كل 6 ساعات
- ✅ تنزيل في الخلفية
- ✅ تثبيت عند الإغلاق
- ✅ الحفاظ على جميع البيانات

## 📊 تسجيل الأخطاء

- ✅ تسجيل محلي للأخطاء
- ✅ إرسال دوري إلى GitHub Issues (كل 6 ساعات)
- ✅ معلومات نظام كاملة
- ✅ تجميع الأخطاء المتشابهة

## 🎯 المتطلبات

- Windows 10/11 (64-bit)
- 4GB RAM (موصى به 8GB)
- 500MB مساحة تخزين
- اتصال إنترنت (للتحديثات وتسجيل الأخطاء)

## 🐛 الإبلاغ عن المشاكل

إذا واجهت مشاكل:

1. تحقق من [GitHub Issues](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/issues)
2. افتح Issue جديد مع التفاصيل
3. التطبيق يُرسل الأخطاء تلقائياً كل 6 ساعات

## 📝 ملاحظات إضافية

### مسارات التخزين

- **البيانات:** `C:\Users\<username>\AppData\Roaming\DesktopManagementSystem\`
- **النسخ الاحتياطية:** داخل مجلد البيانات
- **سجلات الأخطاء:** `userData/error-logs/`

### أمان البيانات

- جميع البيانات محفوظة محلياً
- البيانات الحساسة مشفرة
- نسخ احتياطي تلقائي (آخر 10 نسخ، مدة 30 يوم)
- الحفاظ على البيانات عند الترقية

---

**Made with ❤️ by Desktop Management System Team**
```

---

## 📊 المراقبة والصيانة

### مراقبة الأخطاء

#### GitHub Issues

```bash
# الأخطاء ستظهر هنا تلقائياً:
https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/issues

# ستكون مُصنفة بـ:
- Label: bug
- Label: auto-report
- Label: platform:win32 (أو platform:darwin)
```

#### مثال على Issue تلقائي:

```markdown
## 🐛 تقرير خطأ تلقائي

**عدد المرات:** 5
**الأنظمة المتأثرة:** win32
**الإصدارات:** 1.0.0
**آخر حدوث:** 2025-10-29T15:30:00.000Z

### 📋 تفاصيل الخطأ

**النوع:** TypeError
**الرسالة:** Cannot read property 'map' of undefined

### 📚 Stack Trace
```

TypeError: Cannot read property 'map' of undefined
at TenderList.render (TenderList.tsx:45:12)
at ...

```

### 🖥️ معلومات النظام

- **المنصة:** win32 (x64)
- **إصدار النظام:** 10.0.19045
- **إصدار Electron:** 38.0.0
- **إصدار Node:** 18.20.5
- **إصدار التطبيق:** 1.0.0

### 📊 تفاصيل الحدوث

1. 2025-10-29T15:30:00.000Z - win32
2. 2025-10-29T14:15:23.000Z - win32
3. ...
```

### مراقعة التحديثات

```bash
# في ملف السجل (Audit Log)
# يتم تسجيل:
- تاريخ الفحص
- النتيجة (update-available / update-not-available)
- التحميل
- التثبيت
```

### نصائح الصيانة

**أسبوعياً:**

- [ ] راجع GitHub Issues الجديدة
- [ ] صنّف الأخطاء حسب الأولوية
- [ ] أصلح الأخطاء الحرجة

**شهرياً:**

- [ ] راجع إحصائيات الأخطاء
- [ ] حلل الأنماط المتكررة
- [ ] خطط للتحسينات

---

## 🔧 استكشاف الأخطاء وإصلاحها

### المشكلة: الأخطاء لا تُرسل إلى GitHub

**الحل:**

```bash
# 1. تحقق من Token
echo $GITHUB_ERROR_REPORT_TOKEN

# 2. تحقق من الإعدادات في DevTools
window.electronAPI.invoke('error-reporter-stats').then(console.log)
# يجب أن يُظهر: reportingEnabled: true

# 3. فحص السجلات المحلية
# Windows: C:\Users\<username>\AppData\Roaming\DesktopManagementSystem\error-logs\

# 4. إرسال يدوي للاختبار
window.electronAPI.invoke('error-reporter-send-now').then(console.log)
```

### المشكلة: التحديثات لا تعمل

**الحل:**

```bash
# 1. تحقق من electron-builder.yml
# - owner و repo صحيحان
# - Release منشور على GitHub

# 2. تحقق من latest.yml
# في: build/electron/latest.yml
# يجب أن يحتوي على:
# - version
# - files
# - path (URL صحيح)

# 3. فحص يدوي
# في DevTools Console:
const { autoUpdater } = require('electron-updater');
autoUpdater.checkForUpdatesAndNotify();
```

### المشكلة: البيانات ضاعت بعد الترقية

**الحل:**

```bash
# 1. تحقق من مسار userData
# Windows: C:\Users\<username>\AppData\Roaming\DesktopManagementSystem\

# 2. تحقق من وجود config.json
# يجب أن يكون في المسار أعلاه

# 3. استعادة من النسخ الاحتياطي
# في التطبيق → الإعدادات → النسخ الاحتياطي → استعادة

# 4. إذا لم تجد البيانات
# تحقق من:
# C:\Users\<username>\AppData\Roaming\DesktopManagementSystem-Dev\
# (نسخة التطوير قد تكون في مسار مختلف)
```

### المشكلة: التطبيق بطيء أو يتجمد

**الحل:**

```bash
# 1. فحص حجم البيانات
# في DevTools Console:
const store = await window.electronAPI.store.getAll();
const sizeInBytes = JSON.stringify(store).length;
console.log('Data size:', (sizeInBytes / 1024 / 1024).toFixed(2), 'MB');

# 2. تنظيف النسخ الاحتياطية القديمة
# (تلقائي - مصفوفة 10×30)
# يدوي: الإعدادات → النسخ الاحتياطي → تنظيف

# 3. إعادة بناء قاعدة البيانات
# في التطبيق → الإعدادات → الصيانة → إعادة بناء
```

---

## 📈 مؤشرات النجاح

### بعد النشر مباشرة (أول 24 ساعة)

- [ ] المثبت يعمل بدون أخطاء
- [ ] التطبيق يفتح ويعمل
- [ ] البيانات تُحفظ وتُسترجع
- [ ] لا توجد أخطاء حرجة في GitHub Issues

### بعد أسبوع

- [ ] عدد المستخدمين النشطين
- [ ] عدد الأخطاء المُبلغ عنها
- [ ] معدل نجاح التحديثات
- [ ] لا توجد شكاوى من فقدان البيانات

### بعد شهر

- [ ] معدل الاحتفاظ بالمستخدمين > 60%
- [ ] معدل الأخطاء < 0.1% per user
- [ ] رضا المستخدمين > 80%
- [ ] جميع الأخطاء الحرجة مُصلحة

---

## ✅ قائمة التحقق النهائية

قبل النشر، تأكد من:

### الإعدادات

- [ ] `.env.production` موجود ويحتوي على Token صحيح
- [ ] `package.json` version = 1.0.0
- [ ] `electron-builder.yml` owner/repo صحيحان
- [ ] Git tag v1.0.0 موجود

### البناء

- [ ] `npm run build:electron` يعمل بدون أخطاء
- [ ] المثبت (.exe) موجود في build/electron/
- [ ] latest.yml موجود
- [ ] حجم المثبت معقول (< 150MB)

### الاختبار

- [ ] المثبت يعمل على جهاز نظيف
- [ ] التطبيق يفتح ويعمل
- [ ] البيانات تُحفظ بنجاح
- [ ] النسخ الاحتياطي يعمل
- [ ] تسجيل الأخطاء يعمل

### GitHub

- [ ] Release منشور على GitHub
- [ ] الملفات مرفوعة (.exe + latest.yml)
- [ ] Release Notes كاملة
- [ ] Token لديه صلاحيات repo

### التوثيق

- [ ] README محدّث
- [ ] CHANGELOG محدّث
- [ ] دليل المستخدم جاهز
- [ ] Release Notes جاهزة

---

## 🎉 تهانينا!

إذا اتبعت جميع الخطوات، فإن تطبيقك الآن:

✅ يُسجل الأخطاء ويُرسلها إلى GitHub تلقائياً
✅ يستقبل التحديثات من GitHub بشكل آمن
✅ يحمي بيانات المستخدمين من الفقدان
✅ يحتفظ بالبيانات عند الترقية

**جاهز للنشر الإنتاجي! 🚀**

---

## 📞 الدعم والمساعدة

### للمطورين

- [GitHub Repository](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-)
- [GitHub Issues](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/issues)
- [Pull Requests](https://github.com/anmar534/Desktop-Management-System--Community---Copy---Copy-/pulls)

### للمستخدمين

- دليل المستخدم: README.md
- الإبلاغ عن مشاكل: GitHub Issues
- التطبيق يُرسل الأخطاء تلقائياً

### التوثيق الإضافي

- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) - دليل شامل
- [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md) - قائمة تحقق كاملة
- [QUICK_START_DEPLOYMENT.md](QUICK_START_DEPLOYMENT.md) - نشر سريع

---

**تم إنشاء الدليل بواسطة:** Claude Code
**آخر تحديث:** 2025-10-29
**النسخة:** MVP 1.0.0
**الحالة:** ✅ Production Ready
